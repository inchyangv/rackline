# HashCredit 환경설정 템플릿 (SPV 모드 기준)
# 이 파일을 .env 로 복사한 뒤 값을 채우세요.
#
# 주의:
# - 비밀값(PRIVATE_KEY, BITCOIN_RPC_PASSWORD 등)은 절대 git에 커밋하지 마세요.
# - 컨트랙트는 "별도 배포" 후 주소만 받아서 아래에 채웁니다.

# ============================================
# EVM 네트워크 설정 (Creditcoin testnet)
# ============================================
RPC_URL=https://rpc.cc3-testnet.creditcoin.network
# offchain(prover/api)는 EVM_RPC_URL 이름을 쓰는 경우가 있어 같이 둡니다.
EVM_RPC_URL=https://rpc.cc3-testnet.creditcoin.network
CHAIN_ID=102031
# 운영키(트랜잭션 서명용). 테스트넷 가스가 있어야 합니다.
PRIVATE_KEY=

# ============================================
# 컨트랙트 주소 (별도 배포 후 채우기, SPV 필수 4개)
# ============================================
HASH_CREDIT_MANAGER=
LENDING_VAULT=
CHECKPOINT_MANAGER=
BTC_SPV_VERIFIER=

# ============================================
# (옵션) 로컬/스크립트용 Stablecoin 설정
# ============================================
# 비워두면 배포 스크립트가 TestnetMintableERC20을 자동 배포합니다.
# 이미 배포된 ERC20을 재사용하려면 주소를 넣으세요.
STABLECOIN_ADDRESS=
# Vault 초기 유동성(6 decimals). 기본값: 1_000_000 * 1e6 = 1000000000000
INITIAL_LIQUIDITY=1000000000000

# ============================================
# Bitcoin Core RPC 설정 (SPV 필수)
# ============================================
# Bitcoin Core RPC endpoint.
# - 본인 노드: 보통 testnet `http://127.0.0.1:18332`
# - 퍼블릭 노드(인증 없음): 아래 publicnode 예시 사용 가능
BITCOIN_RPC_URL=https://bitcoin-testnet-rpc.publicnode.com
BITCOIN_RPC_USER=
BITCOIN_RPC_PASSWORD=

# ============================================
# SPV relayer(Worker) 튜닝 값 (선택)
# ============================================
SPV_CONFIRMATIONS=6
SPV_POLL_INTERVAL=60

# ============================================
# SPV 라이브 데모 입력값 (script/demo_live_spv.sh)
# ============================================
BORROWER_EVM=
BORROWER_BTC_ADDRESS=
BTC_TXID=
BTC_VOUT=0
# 선택: 비워두면 스크립트가 BTC_TXID로부터 자동 추론합니다.
TARGET_HEIGHT=
# 선택: 비워두면 스크립트가 안전하게 자동 선택합니다.
CHECKPOINT_HEIGHT=

# 데모 편의 옵션(선택)
SKIP_DEPLOY=0
SKIP_REGISTER=0
