# HashCredit SPV Worker(Prover) 환경설정 템플릿
# 이 파일을 offchain/prover/.env 로 복사한 뒤 값을 채우세요.
#
# Railway 배포 시에는 .env 파일 대신 Railway Variables/Secrets에 동일 키를 넣으면 됩니다.
# 비밀값(PRIVATE_KEY, BITCOIN_RPC_PASSWORD, DATABASE_URL 등)은 절대 git에 커밋하지 마세요.

# ============================================
# DB (Railway에서는 Postgres를 붙이고 DATABASE_URL을 참조)
# ============================================
# 로컬 기본값(없으면 코드가 sqlite:///./spv_relayer.db 로 사용):
DATABASE_URL=sqlite:///./spv_relayer.db

# ============================================
# Bitcoin Core RPC (SPV 필수)
# ============================================
BITCOIN_RPC_URL=http://127.0.0.1:18332
BITCOIN_RPC_USER=
BITCOIN_RPC_PASSWORD=

# ============================================
# EVM (Creditcoin testnet)
# ============================================
EVM_RPC_URL=https://rpc.cc3-testnet.creditcoin.network
CHAIN_ID=102031
PRIVATE_KEY=

# ============================================
# Contracts (SPV)
# ============================================
HASH_CREDIT_MANAGER=
CHECKPOINT_MANAGER=

# ============================================
# Watched addresses (필수)
# ============================================
# 아래 3개 중 "하나만" 사용하세요. Railway에서는 ADDRESSES_JSON_B64 권장.
ADDRESSES_FILE=
ADDRESSES_JSON=
ADDRESSES_JSON_B64=

# ============================================
# Relayer tuning
# ============================================
SPV_CONFIRMATIONS=6
SPV_POLL_INTERVAL=60
SPV_RUN_ONCE=false

# start-worker.sh가 아래 값을 그대로 추가 인자로 붙입니다.
RELAYER_ARGS=
