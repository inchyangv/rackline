# Docker Compose defaults (NO SECRETS)
#
# Purpose:
# - Make `docker compose up` work on a fresh clone without requiring local .env files.
# - Keep all sensitive values empty. Use Railway Secrets or local ignored env files for real runs.

# DB (compose sets DATABASE_URL via service env; keeping a local default too)
DATABASE_URL=

# Bitcoin RPC (testnet public endpoint; no auth required)
BITCOIN_RPC_URL=https://bitcoin-testnet-rpc.publicnode.com
BITCOIN_RPC_USER=
BITCOIN_RPC_PASSWORD=

# Creditcoin EVM (CC3 testnet)
EVM_RPC_URL=https://rpc.cc3-testnet.creditcoin.network
CHAIN_ID=102031
PRIVATE_KEY=

# Contracts (CC3 testnet deployed)
HASH_CREDIT_MANAGER=0x3cfb7fcf0647c78c3f932763e033b6184d79a936
CHECKPOINT_MANAGER=0xe792383beb2f78076e644e7361ed7057a1f4cd88

# Watched addresses (compose-safe default: empty list)
ADDRESSES_JSON=[]
ADDRESSES_JSON_B64=
ADDRESSES_FILE=

# Relayer tuning
SPV_CONFIRMATIONS=6
SPV_POLL_INTERVAL=60
SPV_RUN_ONCE=false
RELAYER_ARGS=

