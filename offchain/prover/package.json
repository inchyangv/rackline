{
  "name": "hashcredit-prover",
  "private": true,
  "description": "Rackline SPV worker/prover (Dockerfile build; Python runtime).",
  "scripts": {
    "start": "echo \"This package is deployed via Dockerfile.\" && exit 0"
  }
}

