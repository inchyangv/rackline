# GPU-019 — ProviderAdapter 계약·capability·mock harness

```text
Ticket: GPU-019 / 상태: DONE (LOCAL) / 실행자: Claude Code fork (Opus 5, 1M) / 기준 commit: 4108909 + 작업 트리
증거 수준: LOCAL — mock adapter + 비식별 fixture. 실제 파트너 API/계정 없음.
executionProfile: LOCAL_MOCK (mock) / verificationMethod: OFFCHAIN_ASSERTION (adapter 출력 고정값) / nativeStatus: NOT_REQUIRED (고정값)
earningsProvenance: fixture = SIMULATED / controlGrade: adapter는 E0/E1 관측만 보고(E2 출력 불가) / cashState: NONE (claim 성공 ≠ 현금)
공식 통합: 없음 — ProviderAdapter는 사업자료 capability만; 공식 proof capability는 GPU-079 worker (assert_native_capable은 항상 raise)
```

## 바뀐 동작과 파일

| 파일 | 변경 |
| --- | --- |
| `offchain/gpu/hashcredit_gpu/providers/types.py` (신규) | `Support{SUPPORTED,UNSUPPORTED,UNKNOWN}`(UNKNOWN=unsupported), `Capabilities`(6 op + `source_support`; read/write 독립), `Cursor`/`Page[T]`/`Window`(max_age, as_of), `AccountRef`, 관측 모델(`AssetListing`, `RevenueObservation`(gross−deductions=net 검증), `SettlementObservation`, `ControlStateObservation`(observed_grade ∈ {E0,E1}), `ClaimResult`(cash_state=NONE 고정), `ControlChangeResult`(control_grade_effect=NONE 고정)) — 모든 관측은 `provenance=PROVIDER_API`, `trust=ASSERTED`, `verification_method=OFFCHAIN_ASSERTION`, `native_status=NOT_REQUIRED`를 `Literal`로 고정(호출자가 승격 불가). Money = 기존 `domain.Money`(정수 문자열 + AssetRef) |
| `providers/errors.py` (신규) | `UnsupportedOperation`, `UnsupportedChainError`, `MalformedResponse`, `MissingUnit`, `ExpiredData`, `RateLimited(retry_after)`, `AuthError`, `NotProductionAdmissible`, `NotNativeCapable` |
| `providers/base.py` (신규) | `ProviderAdapter` ABC(`identity`, `capabilities`, `list_assets`, `fetch_revenue`, `fetch_settlements`, `get_control_state`, `claim_revenue`, `request_control_change`), namespace/chain/capability 가드, `check_freshness`, `assert_production_admissible`(mock·slug·비PRODUCTION profile·SIMULATED/UNKNOWN 매출·source_support≠SUPPORTED → raise), `assert_native_capable`(항상 raise) |
| `providers/mock.py` (신규) | `MockProviderAdapter`: slug `mock-*`/`mockdepin-testonly` 강제, PRODUCTION profile 거부, `is_mock=True`, fixture 기반 결정적 페이지네이션(offset cursor, 잘못된 cursor 거부), write는 fixture에서 SUPPORTED로 켜지 않으면 UNSUPPORTED, idempotency key, rate-limit 시뮬레이션, 단위/체인/decimal 누락·float·revision 역행·naive timestamp → 예외 |
| `providers/__init__.py` (신규) | export |
| `offchain/gpu/tests/fixtures/providers/mockdepin-testonly.json` (신규) | 비식별 fixture(`kind=DEIDENTIFIED_PROVIDER_FIXTURE`, Sepolia TEST-USDC, 자산 5, 매출 3(inv-A rev1/rev2, inv-B), 정산 1, control state E1·borrowerCanChangeReceiver=true) |
| `offchain/gpu/tests/test_provider_contract.py` (신규) | 29 tests, adapter factory로 파라미터화(실제 adapter는 GPU-020/021에서 등록) |

## 적용한 결정 ID·버전 / 가정 / OPEN 항목

- 적용: R2-D04(보조/API 사실 ≠ source fact — 관측 필드 Literal 고정), R2-D05(claim 성공 ≠ 현금, control change 수락 ≠ E2), R2-D08(API 정상이어도 `source_support` 미지원이면 admission 거부), R2-D12(NATIVE_TESTNET/SIMULATED는 production 부적격), GPU-076 §8(사업자료 capability ↔ 공식 proof capability 분리), PIVOT §8.1(unsupported 명시, read≠write).
- 가정: `Page` offset cursor는 mock 전용 형식; 실제 adapter는 provider 토큰을 opaque로 전달. `Window.max_age_seconds` 기본 24h는 호출자(GPU-022/023)가 정책값으로 덮어씀.
- OPEN: 없음(이 티켓). 실제 Aethir/GPU.net capability 값은 GPU-004/005 자료 후 GPU-020/021.

## 완료 기준별 확인 결과

| 완료 기준 | 결과 |
| --- | --- |
| 6개 op의 입력·출력·cursor·에러·capability 타입 정의; read/write 독립; unknown=unsupported | [통과] `types.py`/`errors.py`; `test_identity_and_namespace`, `test_unsupported_write_raises`(UNKNOWN도 raise) |
| mock는 LOCAL 태그·별도 namespace로만 동작, production admission/decision 생성 불가 | [통과] `test_mock_adapter_is_local_and_separately_namespaced`, `test_mock_cannot_claim_production_profile_or_real_slug`, `test_mock_to_production_path_refused`, `test_admissibility_needs_every_flag_and_still_not_native` |
| malformed response·누락 단위·불명 chain은 성공 처리 안 함 | [통과] `test_malformed_response_never_succeeds` ×7 (asset/decimals 누락, float, 미지원 chain, 필드 누락, net 불일치, naive time), `test_bad_cursor_rejected` |
| 검증: pagination, unsupported write, expired data, revision, mock→production 거부 | [통과] `test_pagination_cursor_is_stable_and_complete`, `test_unsupported_write_raises`, `test_expired_data_rejected`, `test_revision_must_be_monotonic_per_ref`, `test_mock_to_production_path_refused` |
| R2: 사업자료 capability와 공식 proof capability 분리 | [통과] `assert_native_capable` 항상 raise; `test_provider_adapter_is_never_native_capable`; 관측 `native_status=NOT_REQUIRED` 고정(`test_observation_trust_fields_cannot_be_upgraded`) |
| R2: API 정상/공식 source 미지원, genuine testnet/mock provider도 production 부적격 | [통과] `test_api_healthy_but_official_source_unsupported_is_refused`, `test_genuine_testnet_profile_with_simulated_revenue_is_not_production` |
| 파트너 계정 없이 완료 | [통과] fixture 비식별(`test_fixture_is_deidentified`) |

## 실행 명령·cwd·exit code·test 개수·중요 결과

| # | cwd | 명령 | exit | 결과 |
| --- | --- | --- | --- | --- |
| 1 | root | `.venv-py313/bin/python -m pytest offchain/gpu/tests/test_provider_contract.py -q` | 0 | **29 passed** |
| 2 | root | `.venv-py313/bin/python -m pytest offchain/gpu/tests -q` (PY-GPU, 임시 PG) | 1 | 84 passed / **2 failed** — 실패 2건은 이 티켓 무관: `test_event_ledger.py::test_head_is_0002_*`, `::test_backup_restore_*`가 head `0002`를 고정했는데 병렬 GPU-017이 migration `0003`을 추가(통합 담당이 GPU-017 커밋 시 갱신 필요). 기준선 57 → provider 29 추가 |
| 3 | offchain/gpu | `ruff check hashcredit_gpu/providers tests/test_provider_contract.py` / `ruff format` | 0 | All checks passed (PEP 695 `class Page[T]`를 `Generic[T]`로 교체 — 패키지는 py311 대상) |
| 4 | offchain/gpu | `mypy hashcredit_gpu/providers` | 0 | no issues (5 files) |
| 5 | root | `git diff --check` | 0 | clean |

## 실패/미실행과 이유

- 실제 파트너 adapter(Aethir/GPU.net) 미구현 — GPU-004/005 DD 자료 후 GPU-020/021.
- PY-GPU 전체 2건 실패는 GPU-017 병렬 산출물(migration 0003)과 GPU-016 테스트의 head 고정 충돌; 이 티켓 파일과 무관.

## 외부 검증

없음.

## 남은 작업·차단 입력·다음 READY 티켓

- 차단 입력: 없음(이 티켓).
- 다음 READY: GPU-022(수집 cursor·backfill·webhook; 선행 016/019), GPU-020/021은 DD 대기.

## 재개 시 필요한 파일·실행 중 프로세스/포트·변경 소유권

- 파일: `offchain/gpu/hashcredit_gpu/providers/*`, `offchain/gpu/tests/test_provider_contract.py`, fixture.
- 프로세스/포트: 없음. 소유권: providers 패키지 = GPU-019; TICKET 상태·커밋은 통합 담당.

## Review

(독립 리뷰 세션이 작성)
