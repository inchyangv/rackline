# GPU-076 — native 증명 범위·source 금융 사건·매출채권 의미 계약

```text
Ticket: GPU-076 / 상태: DONE (SPEC v1; 파트너 의존 항목 OPEN EC-O01~O03) / 실행자: Claude Code (Opus 5, 1M) / 기준 commit: 92fe030 + 작업 트리
증거 수준: LOCAL (cross-language 벡터 실행) — native proof 실행 없음
공식 통합: GPU-075 고정 artifact 참조(SDK 0.18.0 ProofResult shape, EvmV1Decoder 필드), manifest cc3-testnet.sepolia.v1
```

## 바뀐 동작과 파일

| 파일 | 변경 |
| --- | --- |
| `docs/gpu/attestcoin/evidence-contract.md` (신규) | 4단계 체인과 각 단계의 증명 범위/비증명 범위, ProofEnvelope(비신뢰), proof-bound locator·ID 계약(`sourceEventId` = keccak(abi.encode(keccak(envId), chainKey, height, txIndex, logOrdinal)); cache key ≠ consumption key; economic ID 불변), 내부 제안 event 6종 vs 외부 ABI 분리, payout-only 파트너는 history 전용·admission OPEN, 발행 권한/state change/binding/단위/순액/revision/중복 정책, freshness·누락 방어(checkpoint 규칙, revision gap, selective disclosure, lag 0), reorg vs 분쟁, 분리표, OPEN 3, 벡터 목록 |
| `docs/gpu/attestcoin/proof-to-business-mapping.md` (신규) | SDK 응답·검증 bytes·destination tx·업무 분류 필드별 trust(PROVEN/CLAIMED) 매핑 + EV-01 worked example |
| `test/fixtures/gpu/attestcoin/source-events-v1.abi.json` (신규) | `PROPOSED_INTERNAL_SOURCE_EVENTS` 6 events(topic0 = keccak(signature) 검증), 외부 ABI pending 명시 |
| `test/fixtures/gpu/attestcoin/evidence-vectors-v1.json` (신규) | EV-01~EV-12 리뷰 벡터 |
| `test/fixtures/gpu/attestcoin/canonical-id-vectors-v1.json` (신규) | sourceEventId 5, economicEventId hash 2, amount 4 (uint256 max, 2^53+1) |
| `offchain/attestcoin/test/canonical.test.ts` (신규) | TS가 Python 벡터와 동일 ID/금액 도출 (3 tests) |
| `config/gpu/schema/domain-v1.schema.json` | locator/blockNumber 정수에 `maximum: 2^53-1` 명시 |

## 적용한 결정 ID·버전 / 가정 / OPEN 항목

- 적용: R2-D02/D04/D05/D06/D07/D08; GPU-075 사실(V1 encoding에 source timestamp 없음, `calculateTxIndex` on-chain, 두 hostname 별도); GPU-011 ID 체계; GPU-012 AC-07(source 수령 ≠ 상환).
- 발견: 벡터 JSON에 uint64 최대값을 숫자로 넣자 JS 파싱에서 2^64로 손상 → locator 문자열화 + schema 상한 명시(정밀도 규칙의 실증).
- OPEN: EC-O01(파트너의 native 증명 가능한 obligation/assignment 사건), EC-O02(파트너 checkpoint 존재 여부/형태), EC-O03(파트너 ABI/decimals/issuer 역할) — GPU-004/005/008.

## 완료 기준별 확인 결과

| 완료 기준 | 결과 |
| --- | --- |
| 실제 지원 source event 의미·우리 서버 주장·파트너 권리·E2·실제 cash 구분 | [통과] §1, §8 분리표; EV-04/05/08 |
| native 증명 가능한 채무자 사건과 남은 채권 관계 없으면 production 적격화 불가 | [통과] §4 payout-only 규칙 + EC-O01 |
| 현금흐름/NFT 상품 무단 변경·보조 서명만으로 source 사실 대체 없음 | [통과] §8; OUR_ANCHOR/ASSERTED 경계 |
| proof-bound vs claimed 필드 구분; 입증되지 않은 시간으로 freshness 확장 금지; timestamp 미가공 | [통과] §2, §3, mapping §1–§3 |
| checkpoint/revision/누락 방어, lag 0, 예약 | [통과] §6; EV-10/11 |
| reorg vs 분쟁 분리, 확정 destination 채무·송금·상환 보존 | [통과] §7; EV-12 |
| ID 계약(cache key ≠ consumption key, env/chain, verifier/SDK/bytes/txHash 불변, 다중 log, observer 중복 대사) | [통과] §3; canonical 벡터 TS/Python 일치 |
| 검증 벡터: 2 log/1 tx, 1채권 다지급, 1지급 다채권, own transfer, arbitrary anchor, paid 재담보, 다른 차주/토큰/체인, source 확정·destination 미도착 | [통과] EV-01~EV-08 (+09~12) |
| GPU-014/029에 전달 | [통과] 문서 §10, fixture 경로 |

## 실행 명령·cwd·exit code·test 개수·중요 결과

| # | cwd | 명령 | exit | 결과 |
| --- | --- | --- | --- | --- |
| 1 | root | Python 벡터 생성 (eth_abi/eth_utils) | 0 | canonical-id-vectors-v1.json |
| 2 | root | `npm --prefix offchain/attestcoin run test -- --run test/canonical.test.ts` (초기) | 1 | uint64 max JSON 숫자 손상 발견 → 문자열화 |
| 3 | root | `npm --prefix offchain/attestcoin run test -- --run` | 0 | **36 passed** (33 기존 + 3 canonical) |
| 4 | root | `python script/gpu/validate_domain_fixture.py` | 0 | VALID (schema 상한 추가 후) |
| 5 | root | fixture 자기일관성(EV-01 id 재도출, topic0=keccak(signature), signature=name(types)) | 0 | ok |
| 6 | root | `git diff --check` | 0 | |

## 남은 작업·차단 입력·다음 READY 티켓

- 차단 입력: 파트너 ABI/사건(EC-O01~O03).
- 다음 READY: **GPU-014**(선행 011/012/013/076 충족) → GPU-029 → GPU-030/031/078 등.

## Review

(독립 리뷰 세션이 작성)
