# GPU-000 — 재현 환경과 Claude Code 작업 진입점

```text
Ticket: GPU-000 / 상태: DONE / 실행자: Claude Code (Opus 5, 1M) / 기준 commit: fbc5549 (main)
dirty 범위: 기존 미추적 docs/hackathon/{CEIP_EMAIL,FOLLOWUP_EMAIL,MAIL}.md 보존(미변경, 미stage)
증거 수준: LOCAL
executionProfile: n/a (환경 설정 티켓) / verificationMethod: n/a / nativeStatus: NOT_IMPLEMENTED
earningsProvenance: n/a / controlGrade: n/a / cashState: n/a
공식 통합: 없음 — 공식 SDK/ABI/decoder/manifest는 GPU-075에서 고정. 현재 코드에 Attestcoin 구현 0건.
```

## 바뀐 동작과 파일

| 파일 | 변경 |
| --- | --- |
| `CLAUDE.md` (신규, root) | 짧은 진입 지침: 읽기 순서, R2 확정 결정, GPU-074/075 진입, 현재 존재하는 검증 명령만, 증거/인수인계 규칙. TICKET.md 복제 아님 |
| `docs/gpu/execution/BASELINE.md` (신규) | 도구 버전, 재현 설치 절차, 실제 suite 실행 결과, R2 코드 inventory, 기준선 실패 목록 |
| `docs/gpu/execution/GPU-000.md` (신규) | 이 기록 |
| `docs/gpu/execution/baseline/pip-freeze-py313.txt` (신규) | Python 3.13 venv 해석 결과 81 패키지(고정 가능한 의존성 참조) |
| `TICKET.md` | GPU-000 `상태: READY → DONE` (한 줄) |
| `.venv-py313/` (로컬, git 무시) | Python 3.13.7 venv, relayer/prover/api `[dev]` editable 설치, `coincurve==21.0.0` 포함 |

기존 `.venv/`(3.14), `offchain/api/.venv313/`, `keys/`, `.env`(없음), 레거시 컨트랙트/테스트는 변경하지 않았다.

## 적용한 결정 ID·버전 / 가정 / OPEN 항목

- 적용: TICKET.md R2 §0.9 (Creditcoin 원장 + 공식 Attestcoin native 필수, 자체 서명/관리자/SPV/mock 대체 금지).
- 가정: 프로젝트 Python 요구 `>=3.11`이므로 로컬 3.13은 유효. CI는 3.11이며 로컬과 동일 버전을 강제하지 않았다.
- OPEN(다른 티켓): `forge fmt` 드리프트 처리 주체(GPU-054/060), 의존성 lock 형식(GPU-015), README/TECH 현재형 주장 정합화(GPU-074).

## 완료 기준별 확인 결과

| 완료 기준 | 결과 |
| --- | --- |
| 기존 suite를 실행해 통과/실패/환경 차이를 실제 기록 | [통과] BASELINE §3: forge 192/192, py 15+61+9=85 passed, web lint/build OK, `forge fmt --check` FAIL(1 file) 기록 |
| 지원하지 않는 Opus model ID나 무검증 CLI 옵션을 생성하지 않음 | [통과] CLAUDE.md에 model ID/CLI 옵션 없음 |
| 사용자 키·환경값이 로그에 없음 | [통과] `.env` 부재 확인; `.env.example`의 키 이름만 열거, 값 없음; `keys/` 미접근 |
| Python 전용 환경·고정 가능한 의존성·Foundry·Node 버전 기록 | [통과] BASELINE §1–2, pip-freeze 파일 |
| `coincurve` 포함 실제 API test 환경 구성 | [통과] `.venv-py313`에서 PY-API 15 passed (기존 `.venv` 3.14는 coincurve 설치 실패 재현·기록) |
| web lockfile 기준 설치 방법 명시 | [통과] `npm ci --prefix apps/web` (root workspace에 web 없음 명시) |
| CLAUDE.md에 진입·명령·PIVOT 핵심 금융 조건·변경 보존/인수인계만 포함 | [통과] |
| R2: 공식 native 구현 유무와 BTC proof builder를 실제 코드로 구분 | [통과] BASELINE §4: 코드 grep 0건, BTC SPV builder/verifier만 존재; README/TECH 현재형은 planned/unverified로 표기 |
| R2: CLAUDE.md에 R2 결정과 GPU-074/075 진입 포함, ASC 명령을 존재한다고 기록하지 않음 | [통과] "do not exist yet" 명시 |

## 실행 명령·cwd·exit code·test 개수·중요 결과

| # | cwd | 명령 | exit | 결과 |
| --- | --- | --- | --- | --- |
| 1 | root | `forge --version; node --version; npm --version; python3 --version` | 0 | forge 1.4.2, node 22.22.2, npm 10.9.7, 기본 python3 3.14.3 |
| 2 | root | `.venv/bin/python -m pip install "coincurve>=20.0.0"` | 1 | 3.14 sdist 빌드 실패 (hatch LICENSE 오류) — 환경 차이 기록 |
| 3 | root | `/opt/homebrew/bin/python3.13 -m venv .venv-py313 && … pip install -e offchain/{relayer,prover,api}[dev]` | 0 | coincurve 21.0.0 cp313 wheel 설치 |
| 4 | root | `git submodule status` | 0 | forge-std v1.14.0 초기화됨 |
| 5 | root | `forge build --sizes` | 0 | 40 lint warnings, size OK |
| 6 | root | `forge test --summary` | 0 | 12 suites, **192 passed / 0 failed / 0 skipped** (~304 s, invariant 기본 runs) |
| 7 | root | `forge fmt --check` | **1** | `contracts/BtcSpvVerifier.sol` 1건 드리프트 (미수정, 기준선) |
| 8 | offchain/api | `../../.venv-py313/bin/python -m pytest tests/ -q` | 0 | **15 passed**, 2 warnings |
| 9 | offchain/prover | same | 0 | **61 passed** |
| 10 | offchain/relayer | same | 0 | **9 passed**, 1 warning |
| 11 | offchain/api | `../../.venv/bin/python -m pytest tests/ -q --co` | 2 | `test_api.py` 수집 오류(coincurve) — PIVOT §12.1 기준선 재현 |
| 12 | root | `npm ci --prefix apps/web` | 0 | 565 packages; audit 25 findings(미분류) |
| 13 | root | `npm --prefix apps/web run lint` | 0 | clean |
| 14 | root | `npm --prefix apps/web run build` | 0 | built; 652 kB chunk 경고 |
| 15 | root | `git diff --check` | 0 | clean |
| 16 | root | `grep -rniE "attestcoin|usc[-_ ]?sdk|blockprover|…0fd2|gluwa"` (코드 확장자) | — | 코드 0건, 문서만 매치 |

## 실패/미실행과 이유

- `forge fmt --check` 실패: 레거시 컨트랙트 포맷 드리프트. GPU-000은 무관한 정리를 묶지 않으므로 미수정, 기준선으로 기록.
- 미실행: PY-GPU, WEB-TEST, WEB-E2E, V2-E2E, ASC-* — 아직 존재하지 않는 명령 (각 담당 티켓에서 추가).
- Slither 로컬 미설치 — CI 전용, GPU-000 범위 아님.
- 외부 배포 상태(`broadcast/`, 해커톤 주소)는 체인 조회로 검증하지 않았다(범위 밖, 부재도 단정하지 않음).

## 외부 검증

없음 (LOCAL 전용 티켓). 네트워크 broadcast·RPC 쓰기·자금 이동·파트너 연락 없음.

## 남은 작업·차단 입력·다음 READY 티켓

- 차단 입력: 없음.
- 다음 READY: **GPU-074** (SPEC, R2 결정 원장·gap inventory·문서 충돌 정리), **GPU-075** (CODE, 공식 SDK/ABI/manifest·read-only probe — 공식 문서/패키지 접근 필요), GPU-001, GPU-002 (모두 선행 = GPU-000).

## 재개 시 필요한 파일·실행 중 프로세스/포트·변경 소유권

- 파일: `CLAUDE.md`, `TICKET.md` §0/§0.9/§11, `docs/gpu/execution/BASELINE.md`, 이 파일.
- 실행 중 프로세스/포트: 없음.
- 변경 소유권: `docs/gpu/execution/*`와 TICKET.md 상태 갱신은 통합 담당자(현 세션) 단일 편집.

## Review

(독립 리뷰 세션이 작성)
