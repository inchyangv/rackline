# GPU-030 — 역할·provider/account/asset 등록과 지갑 귀속

```text
Ticket: GPU-030 / 상태: DONE / 실행자: Claude Code (Opus 5, 1M) / 기준 commit: 0c1761f + 작업 트리
증거 수준: LOCAL (forge 226 tests)
공식 통합: 없음 (manifestHash 바인딩만; GPU-078이 verifier manifest와 대조)
```

## 바뀐 동작과 파일

| 파일 | 변경 |
| --- | --- |
| `contracts/gpu/ProtocolRoles.sol` | ADMIN/REGISTRAR/UNDERWRITER/GUARDIAN/TREASURY/RELAYER/SERVICER; UNDERWRITER·TREASURY·GUARDIAN 상호 배타(RoleConflict); role별 key epoch + grace 회전 |
| `contracts/gpu/AuthorizationVerifier.sol` | 보조 EIP-712 서명 검증기(GPU-031 몫을 앞당겨 구현): domain(name/version/chainId/verifyingContract 실시간 계산), purpose·subject·signer·keyEpoch·nonce·issuedAt/expiresAt(MAX_TTL), per-(signer,purpose) nonce 소비, role-bound purpose는 role+epoch 검사, 차주 purpose는 epoch 0, EOA(65B, low-s, v∈{27,28}) 또는 ERC-1271 staticcall; 실패 시 구체 custom error bubble |
| `contracts/gpu/ProviderRegistry.sol` | provider 등록(PRODUCTION↔testOnly 일치 강제, encoding 1·chainKey/chainId>0), emitter(topic0별 meaning, codeHash/upgradeAdmin 정보), token(chain별), issuer, policy version, admission 스위치(GUARDIAN/UNDERWRITER), `isAdmitted(manifestHash)` = admission ∧ manifest 일치 ∧ encoding 1 |
| `contracts/gpu/AccountRegistry.sol` | wallet↔borrower(한 번에 하나, release 후 재연결; subject = keccak(WALLET_LINK, borrowerId, wallet, chainid, registry)), account↔borrower 이력(`borrowerOfAccountAt`), provider 존재 검사; grade/ownership 표면 없음 |
| `contracts/gpu/interfaces/IERC1271.sol` | 최소 인터페이스 |
| `test/gpu/GPU030RegistryTest.t.sol` | 15 tests |
| `test/fixtures/gpu/abi/{ProtocolRoles,AuthorizationVerifier,ProviderRegistry,AccountRegistry}.json` | 구현 ABI fixture(총 17) |
| `foundry.toml` | OZ remapping 시도 후 제거(아래 발견) |

발견: OpenZeppelin 5.5(`lib/openzeppelin-contracts`, 941 tracked files)는 `Bytes.sol`/`SignatureChecker.sol`에서 Cancun `mcopy`를 사용 → `evm_version = london`에서 컴파일 불가. 이 프로젝트는 london을 유지(Creditcoin 런타임 EVM 버전은 GPU-080에서 확인)하므로 GPU 컨트랙트는 OZ 없이 자체 최소 구현. GPU-080에서 Cancun 지원이 확인되면 OZ 채택 재검토(GPU-082 영향표).

## 적용한 결정 ID·버전 / 가정 / OPEN 항목

- 적용: R2-D04(보조 서명 ≠ source 사실), R2-D08(unsupported → admission off), R2-D12; GPU-013 §1–§3(역할·서명·epoch); GPU-011 AccountKey/ProviderId; AR-07(도메인 바인딩).
- 가정: `codeHash`/`upgradeAdmin`은 source chain 정보(destination에서 검증 불가) — 기록용; 실제 emitter code 확인은 GPU-080/082.
- OPEN: 없음. (GPU-031은 EvidenceBook + quorum 정책만 남음; AuthorizationVerifier는 여기서 완료.)

## 완료 기준별 확인 결과

| 완료 기준 | 결과 |
| --- | --- |
| proof 서명 복사로 다른 차주에 account를 붙일 수 없음 | [통과] `test_walletLink_signatureCannotBeCopiedToAnotherBorrower` (subject 불일치 → BadSignature), `..._domainBinding_chainAndContract` |
| 등록되지 않은 source 거부 | [통과] `isEmitterRegistered` false·`emitterMeaning` revert, `isAdmitted` manifest 불일치 false, encoding≠1 등록 거부 |
| registry 등록 ≠ GPU 소유권·E2 인증 | [통과] NatSpec + `test_registriesHaveNoControlOrOwnershipSurface`; control grade 표면 없음(GPU-032 별도) |
| 검증: 다른 chain/contract/caller, 동일 nonce·만료, 계약지갑 reject, key rotation, 중복 account, 역할 우회 | [통과] 15 tests (chainId 변경, 다른 verifier/registry, 타인 제출, nonce 재사용, 만료·TTL·purpose·epoch, ERC-1271 accept/reject, malleable s, 중복 wallet/account, epoch 회전 후 구 승인 거부, 비관리자 grant/registrar 우회) |
| backend GPU-017/018과 필드 계약 일치 | [통과] AccountKey/ProviderId 도출 = `domain-model.md` §2(`providerSlug:externalAccountId`), 이력 from/to = DB `asset_provider_assignments`/`provider_accounts` 모델과 동형 |
| R2: source chain·issuer/emitter·event schema·token·account·upgrade 관리자·policy version 등록; 사건 당시/현재 귀속 정책; 가짜 emitter·미승인 proxy upgrade·미등록 issuer 주입 거부 | [통과] ProviderRegistry 필드; `borrowerOfAccountAt`; `test_provider_emitters_tokens_issuers` |

## 실행 명령·cwd·exit code·test 개수·중요 결과

| # | cwd | 명령 | exit | 결과 |
| --- | --- | --- | --- | --- |
| 1 | root | `forge build` (OZ 사용 버전) | 1 | `mcopy` Cancun 오류 → OZ 제거 |
| 2 | root | `forge build` (자체 구현) | 0 | |
| 3 | root | `forge test --match-contract 'GPU030RegistryTest'` | 0 | **15 passed** (초기 4건은 `vm.prank`가 view helper에 소모된 테스트 작성 오류 → 수정) |
| 4 | root | `forge fmt` gpu 파일 / `forge fmt --check` | 0 | 0 diff (vendor 제외) |
| 5 | root | `forge build --sizes` | 0 | ProviderRegistry 7,682B 등 모두 24KB 이하 |
| 6 | root | `FOUNDRY_INVARIANT_RUNS=50 FOUNDRY_INVARIANT_DEPTH=50 forge test --summary` | 0 | **226 tests, 0 failed** |
| 7 | root | `forge inspect <C> abi --json` ×4; `pytest offchain/gpu/tests/test_solidity_abi_fixture.py` | 0 | 3 passed (17 ABI) |
| 8 | root | `git diff --check` | 0 | |

## 남은 작업·차단 입력·다음 READY 티켓

- 다음 READY: **GPU-031**(선행 014/029/030/078 — 078 대기), **GPU-078**(선행 029/030/075/076 충족 ✓), GPU-032(선행 확인), GPU-016, GPU-017/018.

## Review

(독립 리뷰 세션이 작성)
