# GPU-011 — 도메인·ID·단위·경로·API 계약

```text
Ticket: GPU-011 / 상태: DONE (SPEC v1) / 실행자: Claude Code (Opus 5, 1M) / 기준 commit: 8f081a9 (main) + 작업 트리
dirty 범위: 기존 미추적 docs/hackathon/{CEIP_EMAIL,FOLLOWUP_EMAIL,MAIL}.md 보존
증거 수준: LOCAL (schema + fixture + validator 실행)
공식 통합: 없음 (manifestHash 참조만: cc3-testnet.sepolia.v1)
```

## 바뀐 동작과 파일

| 파일 | 변경 |
| --- | --- |
| `docs/gpu/domain-model.md` (신규) | 원칙 7개, ID 체계(경제 ID vs 기술 관측 ID vs proof-bound locator vs cache key), 공통 값 타입(Money=정수 문자열+AssetRef, Timestamp+Provenance), 엔티티 14종의 필드·소유(SOL/DB/API)·PII, 증거 체인 6단계(SourceEvent→ProofRequest→ProofArtifact→NativeVerification→EvidenceConsumption→BusinessEvidence), enum 15종 고정, 시각 의미표, 정정/revision 모델, 온체인/DB/API/브라우저 경계, §0.5 경로표 확정(+`config/gpu/schema/`, `test/fixtures/gpu/domain/`, `script/gpu/`), 제품 API v1 리소스·scope·에러코드 |
| `config/gpu/schema/domain-v1.schema.json` (신규) | JSON Schema 2020-12, 58 `$defs`, `additionalProperties:false`, Money 정규식 `^-?[0-9]+$`, `encoding` const 1, `requiredVerification` const `ATTESTCOIN_NATIVE`, `ProofArtifact.trusted` const false |
| `test/fixtures/gpu/domain/sample-v1.json` (신규) | 22 컬렉션: 1 차주·2 facility·동일 GPU 이동(assignment MOVE)·MIG 자식·1 정산의 2채권 분할·source escrow→bridge leg→destination 수령→repayFor 배분·1 tx 2 log 증거 체인·NATIVE_TESTNET profile 행·같은 chainKey 다른 env·null+provenance·DELTA 정정 |
| `script/gpu/validate_domain_fixture.py` (신규) | schema 검증 + 의미 규칙 12종(배분 합계, paid+unpaid=net, destination-only repayment, consumption 유일성·ACCEPTED 근거·provenTxIndex 일치, OUR_ANCHOR≠PROVEN, TEST_ONLY provider=SIMULATED, profile 격리, chainKey↔chainId env 단일, 열린 assignment 1개, 정정 revision) + `--reader-profile` + `--self-test`(13 negative vectors) |

`jsonschema==4.26.0`을 venv에 설치(GPU-015가 dev 의존성으로 고정 예정).

## 적용한 결정 ID·버전 / 가정 / OPEN 항목

- 적용: R2-D02/D04/D05/D06/D07/D12; PIVOT §6.1–6.2, §7–9; GPU-075 manifest/encoding 사실.
- 가정: `sourceEventId` preimage의 정확한 인코딩과 freshness checkpoint 필드 의미는 GPU-076/029가 확정(문서에 명시). API 구현은 GPU-045.
- OPEN: 없음(이 티켓). 파트너 실제 ID 형식(`providerEventRef`)은 GPU-004/005 자료 후 확정.

## 완료 기준별 확인 결과

| 완료 기준 | 결과 |
| --- | --- |
| 물리 GPU/VM/MIG/container/group/NFT 구분 | [통과] `AssetKind` 7종 + `parentAssetId`; fixture에 PHYSICAL_GPU→MIG_PARTITION |
| 같은 자산의 이동·RMA·여러 account 연결 이력 | [통과] `AssetAssignment{reason: ONBOARD|MOVE|RMA|OFFBOARD, from, to}`; fixture MOVE; 열린 assignment 1개 규칙 |
| DB/API/Solidity 소유 필드와 개인정보 경계 | [통과] §4 Owner/PII 열, §9 경계표 |
| monetary JSON = 정확한 정수 문자열/명시적 decimal | [통과] `Money.amount` 정수 문자열 + `AssetRef.decimals`; float는 schema 거부(self-test) |
| 각 금액의 asset·chain·decimals, 시각의 의미, 경제 ID/관측 ID, revision, 상태·에러 코드 | [통과] §2, §3, §6, §7, §8, §11 |
| 샘플: 한 차주·두 facility·동일 GPU 이동·분할 정산 | [통과] sample-v1.json VALID |
| upstream 부재 → optional/provenance, 임의 채움 금지 | [통과] `jurisdiction`, `custodian`, `sourceCheckpoint` 등 null + Provenance.trust |
| R2: SourceEvent/ProofRequest/ProofArtifact/NativeVerification/EvidenceConsumption 분리 | [통과] §5, schema $defs 6종 |
| R2: destination 환경·chainKey·EVM chain ID·encoding, profile/method/nativeStatus/provenance/controlGrade/cashState 정의 | [통과] `SourceChainRef`, enum 표 |
| R2: testnet 증거의 production 혼입·같은 chainKey 환경별 다른 source·다중 log fixture | [통과] `--reader-profile PRODUCTION` → 8 rejections; sourceChains 2건 chainKey 1; 2 logs/1 tx |
| §0.5 경로표 확정 | [통과] §10 |

## 실행 명령·cwd·exit code·test 개수·중요 결과

| # | cwd | 명령 | exit | 결과 |
| --- | --- | --- | --- | --- |
| 1 | root | `.venv-py313/bin/python -m pip install jsonschema` | 0 | 4.26.0 |
| 2 | root | `python script/gpu/validate_domain_fixture.py` | 0 | VALID, 0 schema / 0 semantic errors, 22 collections |
| 3 | root | `python script/gpu/validate_domain_fixture.py --reader-profile PRODUCTION` | 1 | 8 rejections (NATIVE_TESTNET rows) — 의도된 실패 |
| 4 | root | `python script/gpu/validate_domain_fixture.py --self-test` | 0 | **13/13 negative vectors rejected** |
| 5 | root | `git diff --check` | 0 | |

## 실패/미실행과 이유

- pytest 통합 없음(PY-GPU는 GPU-015에서 package 구성 후); 현재는 스크립트 실행 결과로 기록.
- Solidity struct/ABI 반영은 GPU-029; DB DDL은 GPU-015/016.

## 외부 검증

없음.

## 남은 작업·차단 입력·다음 READY 티켓

- 다음 READY: **GPU-013** (선행 GPU-003/011 충족). GPU-012는 GPU-007 대기, GPU-015는 GPU-013 대기, GPU-076은 GPU-012/013 대기.

## 재개 시 필요한 파일·실행 중 프로세스/포트·변경 소유권

- 파일: `docs/gpu/domain-model.md`, `config/gpu/schema/domain-v1.schema.json`, `test/fixtures/gpu/domain/sample-v1.json`, `script/gpu/validate_domain_fixture.py`.
- schema 변경은 `schemaVersion` bump + fixture 재검증 + GPU-015/029/045 영향 확인.

## Review

(독립 리뷰 세션이 작성)
