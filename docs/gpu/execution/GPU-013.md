# GPU-013 — 권한·통제 약정·상태 전환 계약

```text
Ticket: GPU-013 / 상태: DONE (SPEC v1) / 실행자: Claude Code (Opus 5, 1M) / 기준 commit: d8f3df0 + 작업 트리
증거 수준: n/a (SPEC) + 로컬 일관성 검사
공식 통합: 없음
```

## 바뀐 동작과 파일

| 파일 | 변경 |
| --- | --- |
| `docs/gpu/permissions-and-states.md` (신규) | 액터 9종(키 보관·금지 행위), 인증/서명 6 purpose(EIP-712 domain·nonce·expiry·keyEpoch·ERC-1271), 통제 약정 연산표(create/attest E2/version/observe/revoke/release + 규칙), facility 상태기계(17 전이, 권한·가드), 금지 전이 7, pause 4종 분리(`pauseRepayments` 제품 경로 비노출), 적대 사례 전이표 10, 온체인/오프체인 권한 지침 |
| `config/gpu/schema/facility-transitions-v1.json` (신규) | 기계가독 전이표·금지 규칙·pause 표 |

## 적용한 결정 ID·버전 / 가정 / OPEN 항목

- 적용: R2-D02/D03/D04/D07/D09/D13; PIVOT §4.1–4.3, §5.3, §7 체크리스트, §8.3; AR-07(도메인 없는 서명 금지).
- 가정: `controlObservationMaxAge`·TTL 기본값은 정책값(GPU-007/010 승인). 실제 파트너 권한/E2는 GPU-009/010.
- OPEN: R2-O07(레거시 relayer 재사용 여부)는 §2의 `CONTROL_ATTESTATION`/`RELAY` purpose 도메인 설계와 호환되므로 코드 결정만 남음(GPU-029).

## 완료 기준별 확인 결과

| 완료 기준 | 결과 |
| --- | --- |
| keeper가 payer/borrower를 바꾸거나 임의 beneficiary를 지정할 수 없음 | [통과] §1 oracle 금지 행위, §3 규칙, §6 "Keeper tries to set beneficiary", 전이표에서 `oracle` authority 0건(assert) |
| draw pause와 collection 유지 | [통과] §5 pause 분리; `repay_for` never_blocked_by 명시 |
| cure/dispute/default 전환 | [통과] §4 DELINQUENT↔ACTIVE, DEFAULTED(credit_committee), disputed evidence 행 |
| debt=0 이후 미결 refund/정산 확인 | [통과] §3 release 조건, §6 "Debt = 0 with pending refund" |
| 완제 후 역할 반환 조건 | [통과] §3 release 효과 |
| receiver 변경↔draw/release race, support override, stale observation, 키 교체, 다른 asset, 오래된 release assertion 전이표 | [통과] §6 10 사례 |
| facility 상태와 운영 계정 상태 독립 | [통과] §4 서두 |
| R2: 지갑/약정/심사/통제 서명 ≠ source 사실 검증 | [통과] §2 표 "Never creates" 열 |
| R2: relay gas payer ≠ 매출 발급자/treasury | [통과] §1 분리 규칙 |
| R2: admin/guardian/underwriter/verifier 교체로 native requirement 해제 불가 | [통과] §3 규칙, §4 금지 전이 "lowering requiredVerification", §6 verifier upgrade |

## 실행 명령·cwd·exit code·test 개수·중요 결과

| # | cwd | 명령 | exit | 결과 |
| --- | --- | --- | --- | --- |
| 1 | root | Python 일관성 검사: 전이표 상태 ∈ `FacilityState` enum, 권한 ∈ Role 집합, 모든 상태 참조, 금지 쌍 부재, oracle authority 0건 | 0 | consistent = True |
| 2 | root | `git diff --check` | 0 | |

## 실패/미실행과 이유

- 실제 upstream 권한 승인(GPU-009/010) 없음 — 설계상 별도.

## 남은 작업·차단 입력·다음 READY 티켓

- 다음 READY: **GPU-015** (선행 GPU-000/011/013/075 충족 — 공통 Python package + DB migration; PostgreSQL 필요). GPU-012는 GPU-007(DD/사업 자료) 대기. GPU-076은 GPU-012 대기.

## 재개 시 필요한 파일·실행 중 프로세스/포트·변경 소유권

- 파일: `docs/gpu/permissions-and-states.md`, `config/gpu/schema/facility-transitions-v1.json`.

## Review

(독립 리뷰 세션이 작성)
