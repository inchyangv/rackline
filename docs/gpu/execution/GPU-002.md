# GPU-002 — 알려진 금융·주소 귀속 결함의 재현 벡터

```text
Ticket: GPU-002 / 상태: DONE / 실행자: Claude Code (Opus 5, 1M) / 기준 commit: 7878922 (main) + 작업 트리
dirty 범위: 기존 미추적 docs/hackathon/{CEIP_EMAIL,FOLLOWUP_EMAIL,MAIL}.md 보존(미변경, 미stage)
증거 수준: LOCAL (Foundry EVM 재현)
executionProfile / verificationMethod / nativeStatus / earningsProvenance / controlGrade / cashState: n/a
공식 통합: 없음 (레거시 v1 회계 결함 벡터)
```

## 바뀐 동작과 파일

| 파일 | 변경 |
| --- | --- |
| `docs/gpu/accounting-regressions.md` (신규) | AR-01~AR-08: 결함, 최소 입력, v1 관측값, 올바른 값, 코드 위치, v2 담당 티켓 |
| `test/diagnostic/AccountingRegressions.t.sol` (신규) | 8 vectors. 기본 실행: `observed == v1` **및** `observed != correct` 단언(결함 고정, CI 통과). `RUN_RED_DIAGNOSTICS=true`: `observed == correct` 단언(v1에 대해 의도적 RED, v2 인수 기준) |

레거시 컨트랙트·기존 테스트(`test_repay_interestFirst` 포함)는 수정하지 않았다.

## 적용한 결정 ID·버전 / 가정 / OPEN 항목

- 적용: R2-D13(레거시 보존), R2-D06(AR-05/06의 최신성 규칙), R2-D04(AR-07 패턴을 v2 보조 서명에 복사 금지).
- 가정: AR-03의 "올바른 값"은 자본화 없음(원금 6,000 + 미납이자 500)을 기본으로 두되, 약정으로 자본화를 택하면 Manager/Vault가 동일하게 반영해야 한다는 불변식이 핵심(GPU-003 결정).
- OPEN: AR-04 금리 세그먼트/index 방식(GPU-034), AR-08 virtual offset/min shares 설계(GPU-037).

## 완료 기준별 확인 결과

| 완료 기준 | 결과 |
| --- | --- |
| $5,000·10%·365일·$250 상환의 올바른 잔액 $5,250과 현재 결과 구분 | [통과] AR-01: v1 `getCurrentDebt`=5,000.00 / correct 5,250.00 |
| donation을 실제 EVM으로 재현 또는 미검증 명시 | [통과] AR-08 EVM 재현: 두 번째 LP $2,000 → 1 share=$1,500; $1,000 입금 revert |
| 알려진 버그를 올바른 v2 기대값으로 옮길 수 있음 | [통과] `correct` 상수 + RED 모드가 그대로 v2 인수 실행이 됨 |
| 부분 이자 소실·Manager/Vault 배분 차이·재차입 자본화·APR 소급·stale 한도·과거 증거 재사용·claim domain 누락·share donation 고정 | [통과] AR-01~08 |
| 의도적 red 재현을 기본 CI suite와 분리, xfail/skip으로 수정 주장 금지 | [통과] 기본 모드는 결함을 고정(green); RED는 env flag로만; skip 미사용 |
| 레거시 계약을 v2로 바꾸지 않음 | [통과] contracts/ 무변경 |

## 실행 명령·cwd·exit code·test 개수·중요 결과

| # | cwd | 명령 | exit | 결과 |
| --- | --- | --- | --- | --- |
| 1 | root | `forge test --match-test test_repay_interestFirst` | 0 | 1 passed — 기존 테스트는 결함(이자 0)을 기대값으로 고정하고 있음(기록) |
| 2 | root | `forge test --match-contract 'AccountingRegressionsTest' -vv` | 0 | **8 passed** (pin 모드) |
| 3 | root | `RUN_RED_DIAGNOSTICS=true forge test --match-contract 'AccountingRegressionsTest'` | 1 | **0 passed; 8 failed** — 각 실패 메시지가 v1 값 ≠ 올바른 값을 출력 (AR-01 5000000000 != 5250000000 등) |
| 4 | root | `forge fmt test/diagnostic/AccountingRegressions.t.sol` → `forge fmt --check` (파일) | 0 | |
| 5 | root | `FOUNDRY_INVARIANT_RUNS=50 FOUNDRY_INVARIANT_DEPTH=50 forge test --summary` | 0 | **204 tests, 0 failed** (192 baseline + 4 GPU-001 + 8 GPU-002) |
| 6 | root | `forge fmt --check` (전체) | 1 | 기준선과 동일(`contracts/BtcSpvVerifier.sol`만) |
| 7 | root | `git diff --check` | 0 | |
| 8 | root | 문서 인용 줄번호 대조(`sed -n Np`) | — | 인용 줄이 해당 코드/주석과 일치(AR-01 `:436`으로 정정) |

## 실패/미실행과 이유

- Slither/정적 분석 미실행(범위 밖). 외부 감사 대체 아님.
- v1 배포 컨트랙트에서의 온체인 재현 없음(로컬 EVM 재현으로 충분; 레거시 자금 미접촉).

## 외부 검증

없음.

## 남은 작업·차단 입력·다음 READY 티켓

- 차단 입력: 없음.
- 다음 READY: GPU-003 (SPEC, term sheet·결정 원장) — 선행 확인 필요; GPU-011~013(선행 확인 필요).

## 재개 시 필요한 파일·실행 중 프로세스/포트·변경 소유권

- 파일: `docs/gpu/accounting-regressions.md`, `test/diagnostic/AccountingRegressions.t.sol`.
- 프로세스/포트: 없음.

## Review

(독립 리뷰 세션이 작성)
