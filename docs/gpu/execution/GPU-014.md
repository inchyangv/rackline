# GPU-014 — 경제 사건·정정·정산 대사의 골든 fixture

```text
Ticket: GPU-014 / 상태: DONE (SPEC v1 + reference reconciler + 5 golden fixtures) / 실행자: Claude Code (Opus 5, 1M) / 기준 commit: 7933fc4 + 작업 트리
증거 수준: LOCAL (fixtures executed by the reference reconciler)
공식 통합: 없음 (sourceEventId/proofQueryKey/artifactId는 GPU-076 계약 참조)
```

## 바뀐 동작과 파일

| 파일 | 변경 |
| --- | --- |
| `docs/gpu/evidence-and-reconciliation.md` (신규) | ID 연결도(merge key = economicEventId만), lifecycle 표(earned→settled→paid, 정정=delta), trust/충돌 규칙, 거부 코드 13종, source↔destination 통화 규칙, 합계 필드, fixture 색인 |
| `offchain/gpu/hashcredit_gpu/reconciliation/{__init__,reference}.py` (신규) | 관측→경제 사건 병합(관측 순서), locator 변조/중복 소비/anchor/emitter 거부, receivable lifecycle, settlement/leg/destination/allocation, 예외 목록, 요약 |
| `test/fixtures/gpu/settlements/s-01..s-05.json` (신규) | 골든 fixture 5종(기대 요약 포함) |
| `offchain/gpu/tests/test_settlement_fixtures.py` (신규) | fixture 러너 + 불변식 + ID 비병합 테스트 (6 tests) |

## 적용한 결정 ID·버전 / 가정 / OPEN 항목

- 적용: R2-D04/D05/D06/D07; GPU-076 §3 ID 계약, EV-01~EV-12; GPU-012 AC-07.
- 설계 결정(기록): 사건 적용 순서는 최초 관측 순서(사전순 아님) — 역순 revision은 재정렬하지 않고 예외로 남김; 다른 계정의 assignment는 `ACCOUNT_MISMATCH`가 1차 사유; 우리 anchor log는 native 소비는 되지만 업무 사실 아님.
- OPEN: 파트너 실제 observation 형식(GPU-020/021)이 오면 adapter가 이 관측 스키마로 정규화.

## 완료 기준별 확인 결과

| 완료 기준 | 결과 |
| --- | --- |
| 한 채권 여러 지급·한 지급 여러 채권 배분 | [통과] S-01(inv-A 여러 payout 가능 구조), S-02(settle-2 → inv-A/inv-B) |
| source/destination 통화 차이 | [통과] S-02 (USDC 6,000 source → mUSDT 3,600 destination; debt 5,000→1,400) |
| 부분 취소·refund·이미 사용한 채권 정정 | [통과] S-03 (delta −1,000 후 decision revoked), CANCELLATION 경로 |
| adapter/schema version 변경이 경제 ID를 새로 만들지 않음 | [통과] S-01 3관측→1사건; `test_ids_are_not_merge_keys` |
| 중복 API+서명+chain 관측, 같은 provider event ID의 다른 account/chain, 역순 revision, 완납 재담보, 출처 불명 입금, source만 수령 | [통과] S-01/03/04, S-01(source only) |
| R2: cache ID/artifact ID/source event ID/economic ID 구분; log ordinal ≠ logIndex; 2 log 각각 1회; verify 선점 방지; anchor 부적격; caller height/time/index 변조 | [통과] §1 표; S-02(ordinal 0/1), S-04(inv-E unconsumed → OBSERVED, c7 anchor, c9 tampered, c10 duplicate) |

## 실행 명령·cwd·exit code·test 개수·중요 결과

| # | cwd | 명령 | exit | 결과 |
| --- | --- | --- | --- | --- |
| 1 | root | `python -m pytest offchain/gpu/tests/test_settlement_fixtures.py -q` (초기) | 1 | 사전순 적용 순서 버그 → 관측 순서로 수정; S-04 기대값 2건 정정(사건 수 7, anchor 소비 포함); ACCOUNT_MISMATCH 우선 |
| 2 | root | 동일(최종) | 0 | **6 passed** |
| 3 | root | `python -m pytest offchain/gpu/tests -q` | 0 | **39 passed** |
| 4 | root | `git diff --check` | 0 | |

## 남은 작업·차단 입력·다음 READY 티켓

- 다음 READY: **GPU-029**(선행 011/012/013/014 충족) → GPU-030 → GPU-031/078; GPU-016(선행 014/015 충족).

## Review

(독립 리뷰 세션이 작성)
