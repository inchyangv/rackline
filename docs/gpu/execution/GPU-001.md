# GPU-001 — 공개 관리자 등록/신용 부여 경로와 데모 권한 격리

```text
Ticket: GPU-001 / 상태: DONE / 실행자: Claude Code (Opus 5, 1M) / 기준 commit: 0d8f964 (main) + 작업 트리
dirty 범위: 기존 미추적 docs/hackathon/{CEIP_EMAIL,FOLLOWUP_EMAIL,MAIL}.md 보존(미변경, 미stage)
증거 수준: LOCAL (unit/integration tests with fake RPC reads; in-process forge script run)
executionProfile: n/a / verificationMethod: n/a / nativeStatus: n/a
earningsProvenance: n/a / controlGrade: n/a / cashState: n/a
공식 통합: 없음 (레거시 v1 API/배포 스크립트 권한 격리)
```

## 바뀐 동작과 파일

| 파일 | 변경 |
| --- | --- |
| `offchain/api/hashcredit_api/config.py` | `API_PROFILE` (`production` 기본 / `testnet_demo`). production: 어떤 키(`ADMIN_PRIVATE_KEY`, `DEMO_ADMIN_PRIVATE_KEY`)도 설정되면 **시작 실패**. `ADMIN_PRIVATE_KEY`는 모든 profile에서 거부. demo: `DEMO_ADMIN_PRIVATE_KEY`, `DEMO_ALLOWED_CHAIN_IDS`(mainnet 102030/1 포함 시 거부, `CHAIN_ID`가 allowlist 밖이면 거부), `DEMO_ALLOWED_STABLECOINS`, `DEMO_GRANT_CAP`, `DEMO_AUTH_TTL_SECONDS` |
| `offchain/api/hashcredit_api/evm.py` | 읽기 전용으로 축소: 키/서명/`register_and_grant` 제거, `get_chain_id`/`read_manager_stablecoin`/`read_verifier_pubkey_hash` 추가 |
| `offchain/api/hashcredit_api/demo.py` (신규) | API가 서명하는 **유일한** 모듈. `DemoAdminClient`(demo 키, 주입 가능한 송신기), `demo_router`: `POST /claim/demo-auth-message`, `POST /claim/register-and-grant`. 가드 순서: 형식 → 차주 EVM 서명(차주·BTC 주소·chain id·manager·만료에 바인딩) → 온체인 BTC 링크(`BtcSpvVerifier.borrowerPubkeyHash`) 일치 → RPC chain id == 설정·allowlist·non-mainnet → manager `stablecoin()`이 TEST_ONLY allowlist → cap → 서명 |
| `offchain/api/hashcredit_api/main.py` | `create_app(settings)` 팩토리, 라우트를 `APIRouter`로 이동, demo router는 `testnet_demo`에서만 mount, 엔드포인트 settings를 앱 바인딩(`get_app_settings`), health에 `api_profile` |
| `offchain/api/hashcredit_api/models.py` | `RegisterAndGrantRequest`에 `evm_signature`/`expires_at` 필수; `DemoAuthMessage*` 추가; `HealthResponse.api_profile` |
| `offchain/api/tests/test_gpu001_admin_isolation.py` (신규) | 23 tests |
| `script/DeploySpv.s.sol` | `setAutoGrantCredit`를 `demoAutoGrantAllowed(deployedTestToken, chainid)`로 게이트(스크립트가 배포한 TEST_ONLY 토큰 + chain ∈ {102031, 31337}일 때만); 빈 `STABLECOIN_ADDRESS=`를 미설정으로 처리; `runWith(address)`/`lastManager`/`lastStablecoin` 테스트 훅 |
| `test/DeploySpvAutoGrant.t.sol` (신규) | 4 tests: 정책 헬퍼, demo chain+test token → 1000e6, external stablecoin → 0, mainnet 102030 → 0 |
| `apps/web/src/lib/env.ts`, `apps/web/.env.example` | `VITE_API_PROFILE` (`production` 기본); `isTestnetDemo` |
| `apps/web/src/features/borrow/setup-panel.tsx` | production 빌드는 'register' 단계를 아예 갖지 않음(등록/한도 요청 없음, 버튼 "Verify payout address", 안내 문구). demo 빌드만 `demo-auth-message` → `personal_sign` → `register-and-grant` |
| `offchain/api/README.md`, `.env.example` | profile/변수/가드 문서화 (README의 기존 "API does not send EVM transactions" 주장은 이제 production에서 사실) |

## 적용한 결정 ID·버전 / 가정 / OPEN 항목

- 적용: TICKET §0.3(production GPU 경로에 testnet grant·공개 owner-key API 금지), PIVOT §2.2 P0 두 건, R2-D04(차주 EVM 서명은 보조 wallet-auth일 뿐 신용/증거 생성 아님 — 서명만으로는 grant 불가, 온체인 링크·체인·자산·cap 모두 필요).
- 가정: v1 데모의 자체 등록 UX는 `testnet_demo` profile로만 유지된다. GPU v2 배포에는 이 profile을 사용하지 않는다.
- OPEN: 실서비스(Railway API/Vercel web) secret·profile 변경과 재배포는 GPU-063. 그때까지 재배포하면 기본값이 production이므로 라이브 데모의 자체 등록이 비활성화된다(의도된 fail-closed).

## 완료 기준별 확인 결과

| 완료 기준 | 결과 |
| --- | --- |
| 익명 입력으로 관리자 tx 불가 | [통과] `test_anonymous_request_rejected_before_signing` (422/403, sign_calls 0) |
| 타 차주 입력으로 불가 | [통과] `test_third_party_cannot_register_another_borrower` (403) |
| 가짜 BTC 주소로 불가 | [통과] `test_fake_btc_address_rejected` (형식 400, 미링크 409) |
| chain 변경으로 불가 | [통과] `test_signature_bound_to_chain_and_manager`, `test_rpc_chain_mismatch_rejected`, config `test_demo_refuses_mainnet_chain_ids`/`..._outside_allowlist` |
| external stablecoin 입력으로 불가 | [통과] `test_external_stablecoin_rejected`, `test_empty_stablecoin_allowlist_rejected`; 배포 스크립트 `test_autoGrant_disabled_with_externalStablecoin` |
| 일반 API 프로세스가 owner key 없이 실행 | [통과] `test_register_and_grant_route_absent`, `test_production_rejects_any_admin_key`, `test_read_only_evm_client_has_no_signing_surface`; 실제 lifespan 기동 확인(아래 #9) |
| 서명 함수 미호출 검사 | [통과] 모든 거부 케이스에서 `DemoAdminClient.sign_calls == 0` 및 송신기 미호출 단언 |
| production API에서 endpoint 부재/거부 | [통과] 404 (route 미등록) |
| UI 자동 grant 요청 제거(production) | [통과] `ORDER`에 'register' 없음; lint/build 통과 |
| GPU 배포에 auto grant 미포함 검사 기반 | [통과] `DeploySpv.demoAutoGrantAllowed` + Foundry 테스트; mainnet/external stablecoin에서 0 |
| legacy 조회/상환 UX 영향 기록 | [통과] 아래 "영향" |
| 인증 헤더만 추가하고 owner key를 서버에 두는 수정 회피 | [통과] production에는 키 자체가 없음; demo 키는 별도 이름·별도 profile |

영향(legacy UX): 조회/차입/상환은 API를 쓰지 않으므로 영향 없음. 신규 자체 등록은 production 빌드/프로세스에서 불가(운영자 등록으로 전환). 라이브 v1 데모를 유지하려면 GPU-063에서 `API_PROFILE=testnet_demo`, `DEMO_ADMIN_PRIVATE_KEY`(새 키), `DEMO_ALLOWED_STABLECOINS=[mUSDT]`, `VITE_API_PROFILE=testnet_demo`를 설정해야 한다. 기존 `ADMIN_PRIVATE_KEY`는 반드시 제거(시작 실패).

## 실행 명령·cwd·exit code·test 개수·중요 결과

| # | cwd | 명령 | exit | 결과 |
| --- | --- | --- | --- | --- |
| 1 | offchain/api | `../../.venv-py313/bin/python -m pytest tests/ -q` | 0 | **38 passed** (기존 15 + 신규 23), 4 warnings |
| 2 | root | `forge test --match-contract 'DeploySpvAutoGrantTest' -vv` ×4 | 0 | **4 passed** ×4 (초기 병렬 `vm.setEnv` 경합 → `runWith(address)`로 제거) |
| 3 | root | `forge build --sizes` | 0 | |
| 4 | root | `FOUNDRY_INVARIANT_RUNS=50 FOUNDRY_INVARIANT_DEPTH=50 forge test --summary` | 0 | **196 tests, 0 failed** (192 baseline + 4) |
| 5 | root | `forge fmt --check` | 1 | 기준선과 동일: `contracts/BtcSpvVerifier.sol`만 (이 티켓 파일은 통과) |
| 6 | root | `npm --prefix apps/web run lint` / `run build` | 0 / 0 | |
| 7 | offchain/prover, offchain/relayer | pytest | 0 / 0 | 61 / 9 passed (무변경 확인) |
| 8 | root | `git diff --check` | 0 | |
| 9 | offchain/api | production lifespan 기동(TestClient context, 키 env 제거) | 0 | `/health api_profile=production`, `/claim/register-and-grant` 404, `app.state.demo_admin` 없음 |
| 10 | offchain/api | `ADMIN_PRIVATE_KEY=… python -c "Settings()"` | 1 | ValidationError "ADMIN_PRIVATE_KEY is no longer accepted" |

## 실패/미실행과 이유

- 실제 RPC/체인에 대한 demo 경로 실행 없음(FakeEVM). 실환경 demo는 GPU-063 재배포 시 확인.
- 공개 서버의 secret 회전/재배포 미수행(범위 밖, GPU-063).

## 외부 검증

없음. broadcast/자금/파트너 연락 없음.

## 남은 작업·차단 입력·다음 READY 티켓

- 차단 입력: 없음.
- 다음 READY: **GPU-002** (선행 GPU-000), GPU-003 (SPEC).

## 재개 시 필요한 파일·실행 중 프로세스/포트·변경 소유권

- 파일: `offchain/api/hashcredit_api/{config,demo,main,evm}.py`, `script/DeploySpv.s.sol`, `apps/web/src/features/borrow/setup-panel.tsx`.
- 프로세스/포트: 없음.

## Review

(독립 리뷰 세션이 작성)
