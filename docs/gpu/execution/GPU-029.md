# GPU-029 — Solidity 공통 타입·인터페이스·이벤트

```text
Ticket: GPU-029 / 상태: DONE / 실행자: Claude Code (Opus 5, 1M) / 기준 commit: 890885b + 작업 트리
증거 수준: LOCAL (forge 211 tests, Python fixture tests)
공식 통합: 공식 Solidity 파일 3종 vendoring(sha256 고정, GPU-075 pin과 동일), SDK 0.18.0 selector 대조
```

## 바뀐 동작과 파일

| 파일 | 변경 |
| --- | --- |
| `foundry.toml` | solc 0.8.24 → **0.8.28**(공식 파일 ^0.8.28 요구; 레거시 ^0.8.24 무변경 컴파일, 204 tests 통과 확인), evm `london` 유지, remapping `@gluwa/asc-contracts/contracts/=contracts/gpu/vendor/attestcoin/`, `fs_permissions` read-only `./test/fixtures`, `[fmt] ignore` vendor |
| `contracts/gpu/vendor/attestcoin/{common/EvmV1Decoder,write-ability/common/INativeQueryVerifier,write-ability/common/BlockProverTypes}.sol` + `VENDORED.md` | 바이트 동일 복사(sha256 3종 = GPU-075 pin) |
| `contracts/gpu/types/GpuTypes.sol` | enum 10종(도메인 schema 순서), user-defined value types(FacilityId/ProviderId/AccountKey/SourceEventId/EconomicEventId), AssetRef/SourceChainRef/SourceEventLocator, **NativeProofEnvelope/VerifiedSourceEvent**(native) vs **SupplementaryAssertion/CreditAuthorization**(보조) 타입 분리, FacilityLedgerView, RepayResult(requested/received/applied/fee/interest/principal/excess/newDebt), DrawEvaluation |
| `contracts/gpu/interfaces/*.sol` (13) | IProtocolRoles, IProviderRegistry, IAccountRegistry, IRevenueVerifier(native adapter, fail-closed 오류 목록), IEvidenceBook(소비/dedup), IAuthorizationVerifier, IControlRegistry, IRevenueEscrow(GPU-076 내부 event 6종), IDebtLedger(단일 원장), ILendingVaultV2, ICreditFacilityManager(pauseRepayments 없음, grant 없음), IRiskPolicy(BTC 가격/sats 없음), IRepaymentRouter — NatSpec에 회계 소유권·오류·이벤트 명시 |
| `contracts/gpu/mocks/MockBlockProver.sol` | TEST_ONLY LOCAL_MOCK: 공식 interface 정확 구현(overload 5종), selector 기록, revert 재현, `calculateTxIndex` 공식 의미 |
| `test/gpu/GPU029InterfacesTest.t.sol` | 7 tests: enum ordinal 파리티(`enums-v1.json`), 공식 selector 5종 = SDK ABI, envelope round-trip, canonical sourceEventId = Python/TS 벡터, RepayResult 항등식, 13 interfaceId 상이·비영, native/보조 타입 분리 |
| `test/fixtures/gpu/enums-v1.json`, `test/fixtures/gpu/abi/*.json` (13) | Solidity↔Python 공용 fixture(`forge inspect` 출력) |
| `offchain/gpu/tests/test_solidity_abi_fixture.py` | vendor sha256 pin, ABI에 BTC-era 이름 없음(sats/txid/vout/btc/spv/pubkeyhash), 필수 surface, enum fixture = schema (3 tests) |

## 적용한 결정 ID·버전 / 가정 / OPEN 항목

- 적용: R2-D02/D03/D04/D07/D11/D12; GPU-011 enum/ID; GPU-012 waterfall/RepayResult; GPU-013 roles/pauses; GPU-075 E1(solc)·E2(full interface 사용)·E3(decoder vendoring); GPU-076 event 6종·locator.
- 가정: EVM `london` 유지(PUSH0 미사용) — Creditcoin 런타임 EVM 버전은 GPU-080 실제 배포에서 재확인. 구현 순환 의존 없음(인터페이스만).
- OPEN: ERC-4626 채택 범위(GPU-034에서 명시).

## 완료 기준별 확인 결과

| 완료 기준 | 결과 |
| --- | --- |
| BTC sats/txid/vout가 공통 매출 타입에 없음 | [통과] `test_interface_abis_have_no_btc_fields_and_expected_surface` |
| repayFor의 requested/received/applied/principalPaid/interestPaid/feePaid/excess/newDebt 구분 | [통과] `RepayResult` + ABI 필드 순서 단언 |
| view 검증과 경제 사건 consume 책임 분리 | [통과] `IRevenueVerifier.verifyAndExtract` vs `IEvidenceBook.consume` |
| SOL(GPU029InterfacesTest), forge build --sizes, Python/Solidity 공용 ABI fixture | [통과] 7 / build OK / 13 ABI + enums fixture + 3 Python tests |
| 순환 의존 없음 | [통과] 인터페이스·타입·mock만 |
| R2: NativeProofEnvelope/VerifiedSourceEvent ↔ SupplementaryAssertion/CreditAuthorization 타입 분리; 공식 ABI(075)/event schema(076)/native 검증(078)/소비(031) 책임 분리; SDK wrapper 이름 미복사 | [통과] `verifySingle` 부재 단언; selector 5종 대조 |

## 실행 명령·cwd·exit code·test 개수·중요 결과

| # | cwd | 명령 | exit | 결과 |
| --- | --- | --- | --- | --- |
| 1 | root | solc 0.8.28 시험: `forge build --sizes`, `FOUNDRY_INVARIANT_RUNS=50 … forge test --summary` | 0/0 | 레거시 204 tests 통과, size 미미 변화 |
| 2 | root | `forge build --sizes` (gpu 포함) | 0 | Compiler run successful |
| 3 | root | `forge test --match-contract 'GPU029InterfacesTest'` | 0 | **7 passed** (fs_permissions·overload 해결 후) |
| 4 | root | `FOUNDRY_INVARIANT_RUNS=50 FOUNDRY_INVARIANT_DEPTH=50 forge test --summary` | 0 | **211 tests, 0 failed** |
| 5 | root | `forge fmt --check` | 1 | 기준선 `contracts/BtcSpvVerifier.sol`만; gpu 파일 0 diff |
| 6 | root | `python -m pytest offchain/gpu/tests/test_solidity_abi_fixture.py -q` | 0 | 3 passed |
| 7 | root | `forge inspect <I> abi --json` ×13 | 0 | fixtures |
| 8 | root | `git diff --check` | 0 | |

## 남은 작업·차단 입력·다음 READY 티켓

- 다음 READY: **GPU-030**(선행 013/029), **GPU-016**(014/015), GPU-017/018, GPU-008/060.

## Review

(독립 리뷰 세션이 작성)
