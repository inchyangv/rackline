# GPU-066 — 브랜드·카피·demo·사용자 전환 안내

```text
Ticket: GPU-066 / 상태: IN_PROGRESS (체크포인트 GPU-066.a DONE) / 실행자: Claude Code (Opus 5, 1M)
기준 commit: 92fe030 (main) + 작업 트리 (병행 세션의 config/gpu/schema, offchain/attestcoin 테스트 변경은 미접촉·미stage)
증거 수준: LOCAL (설정·문서) + 외부 변경 1건: GitHub repo 개명(사용자 명시 지시)
executionProfile: n/a / verificationMethod: n/a / nativeStatus: n/a / earningsProvenance: n/a / controlGrade: n/a / cashState: n/a
공식 통합: 없음
```

## 트리거와 범위

사용자 지시(2026-09-14): "이제 진짜 사업화 … git repo도 이름이랑 uri 바꿔서 세팅해놓고, 배포도 내가 내려놨는데, 맞춰놓자. 도메인은 새로 세팅해야할듯?" → GPU-066의 "이름 유지/변경 결정"과 "도메인 이전 계획"에 대한 사용자 결정. 선행 CODE 티켓(GPU-047~050)은 미완료이므로 체크포인트로 분리한다.

- **GPU-066.a (완료)**: 저장소·배포·서비스 식별자를 Rackline으로 통일하고, 죽은 배포 URL을 코드/문서 기본값에서 제거하며, 도메인·재배포 체크리스트를 문서화.
- **GPU-066.b (미착수, 외부 승인/로그인 필요)**: 도메인 등록, Vercel 프로젝트 `rackline` 생성·링크, Railway 프로젝트 `rackline` 생성, 환경변수/secret 설정, DNS, smoke test, README 상태 갱신.
- **GPU-066.c (선행 CODE 후)**: v2 화면 카피·demo 시나리오·legacy 사용자 안내.

## 식별자 결정 (사용자 결정 2026-09-14)

| 범주 | 결정 | 근거 |
| --- | --- | --- |
| GitHub repo | `inchyangv/rackline` (구 `ctc-hashcredit` → 중간에 `hashcredit` → 이번에 `rackline`; GitHub가 구 URL 리다이렉트) | 사용자 지시 |
| 로컬 remote | `https://github.com/inchyangv/rackline.git` | 위와 동일 |
| 배포/서비스/프로젝트 | `rackline-api`, `rackline-prover-legacy`, `rackline-db`, Vercel `rackline`, Railway `rackline` | 배포 식별자는 코드가 아님 |
| 도메인 | 신규 등록 필요. `rackline.com/.io/.xyz`는 2026-09-14 whois 기준 이미 등록됨, `.co`는 미등록; `.finance/.credit/.capital`은 registrar 확인 필요 | 구 `*.studioliq.com` 배포는 사용자가 내림 |
| Python package/CLI/컨트랙트/env 변수 | 유지 (`hashcredit_api`, `hashcredit-prover`, `HashCreditManager`, `HASH_CREDIT_MANAGER`, `VITE_HASH_CREDIT_MANAGER`) | 코드 식별자 전역 치환 금지(R2-D13); GPU-015의 `hashcredit_gpu`도 동일 정책 |
| 로컬 디렉터리 `ctc-btc-miner` | 미변경 | 세션 cwd·메모리 경로가 묶여 있음; 사용자가 세션 밖에서 `mv` 가능 |

## 바뀐 동작과 파일

| 파일 | 변경 |
| --- | --- |
| GitHub (외부) | `gh repo rename rackline -R inchyangv/hashcredit`; description 설정; homepage 비움(내린 배포 URL 제거) |
| `.git/config` (미추적) | `origin` → `https://github.com/inchyangv/rackline.git` |
| `railway-compose.yml` | 서비스 `rackline-api`, `rackline-prover-legacy`(legacy 표시, 선택 배포) |
| `docker-compose.yml` | 컨테이너/DB/volume 이름 `rackline-*`, 기본 DB 자격증명 `rackline` (로컬 dev 전용) |
| `railway.toml` | 주석: 서비스명 `rackline-api`, `API_PROFILE=production`, admin key 금지 |
| `apps/web/src/lib/env.ts` | `apiUrl` 기본값 `''` (죽은 `api-hashcredit.studioliq.com` 제거; 미설정 시 API 요청이 명시적 오류) |
| `apps/web/.env.example` | `VITE_API_URL=` 비움 + 설명 |
| `apps/web/package.json` | name `rackline-web` |
| `apps/web/index.html` | description 문구, og:title/description/type, twitter:card (도메인 없는 항목만) |
| `offchain/api/.env.compose` | `ALLOWED_ORIGINS`에서 죽은 도메인 제거(localhost만) |
| `docs/deploy/RAILWAY.md` | 명명 규칙·상태 주석, 서비스명 치환, §6 도메인 placeholder, **§9 도메인·재배포 체크리스트** |
| `apps/web/README.md` | Deployment 절: Vercel `rackline`, 재링크 필요, `VITE_API_URL` 기본값 없음 |
| `README.md` | Live demo 줄 → repo URL·개명 사실·배포 내림·재배포 경로; Status 표 Frontend "builds; not currently deployed"; CLI 이름 주석 |
| `TECH_DISCORD.md`, `docs/hackathon/SUBMISSION_VALUES.template.md`, `DORAHACKS.md`(로컬) | repo URL → `inchyangv/rackline`; live demo TODO |
| `docs/gpu/execution/ATTESTCOIN_GAP.md` | C18 행 TAKEN_DOWN(owner statement) + change log |
| `TICKET.md` | GPU-066 `TODO → IN_PROGRESS` (한 줄) |

미변경: `offchain/api/pyproject.toml`·`offchain/prover/*`·`script/*`의 `hashcredit-*` CLI/패키지명(코드 식별자), `apps/web/.vercel/project.json`(gitignore; 구 `ctc-hashcredit` 프로젝트 링크 — 재링크 시 삭제), `.github/workflows`(병행 세션 소유), 병행 세션의 미추적/수정 파일.

## 완료 기준별 확인 결과 (GPU-066.a 범위)

| 완료 기준 | 결과 |
| --- | --- |
| 새 logo/도메인/브랜드를 사용자 결정 없이 확정하지 않음 | [통과] repo/서비스명은 사용자 지시; 도메인은 미확정 상태로 placeholder만 |
| 과거 해커톤 실적과 현재 파트너/금융 지원을 구분 | [통과] README/DORAHACKS 문구 유지(2nd place, 파트너십 미주장) |
| BTC/HashCredit/checkpoint/mUSDT 잔존 검색 후 active/legacy/의도 분류 | [부분] 배포/문서 잔존 0건(`studioliq`, `ctc-hashcredit`, `hashcredit-api` 서비스명); 코드 식별자(`hashcredit_api`, `hashcredit-prover` CLI, `HashCreditManager`)는 의도적 유지로 분류 |
| WEB 검증 | [통과] `npm --prefix apps/web run lint` exit 0; `run build` exit 0 (`tsc -b && vite build`) |
| compose 검증 | [통과] `docker compose config --services` → db, rackline-api, rackline-prover-legacy |
| 실제 도메인/DNS 변경은 별도 승인된 rollout 범위 | [준수] 미실행; §9 체크리스트로 준비 |

## 실행 명령·cwd·exit code·중요 결과

| # | cwd | 명령 | exit | 결과 |
| --- | --- | --- | --- | --- |
| 1 | root | `git remote -v`, `gh auth status`, `gh repo view` | 0 | 로컬 remote `ctc-hashcredit`, GitHub 실제 이름 `hashcredit`, homepage 내린 URL |
| 2 | root | `gh repo view inchyangv/rackline` | 1 | 이름 사용 가능(404) |
| 3 | root | `gh repo rename rackline -R inchyangv/hashcredit --yes`; `gh repo edit … --homepage ""`; `git remote set-url origin …/rackline.git` | 0 | `nameWithOwner: inchyangv/rackline`, homepage 빈 값 |
| 4 | root | grep 구 식별자(`studioliq`, `ctc-hashcredit`, `hashcredit-api/-prover/-db`) | — | 배포·문서 잔존 치환; 코드 식별자만 남음 |
| 5 | root | `vercel whoami`, `railway whoami` | 1 | 둘 다 로그아웃 → 프로젝트 생성은 GPU-066.b(사용자 로그인 후) |
| 6 | root | `whois` 후보 도메인 | 0 | `.com/.io/.xyz` 등록됨, `.co` 미등록, 나머지 미확인 |
| 7 | root | `npm --prefix apps/web run lint && run build` | 0 | 통과 |
| 8 | root | `docker compose config --services` | 0 | 3개 서비스 |
| 9 | root | `git diff --check` | 0 | clean |

## 실패/미실행과 이유

- 도메인 등록·Vercel/Railway 프로젝트 생성·env/secret·DNS·배포: 사용자 계정 로그인과 도메인 선택이 필요 → GPU-066.b.
- `apps/web/.vercel/project.json`(gitignore)은 구 프로젝트를 가리킴; 재링크 시 삭제 필요(사용자 로컬 파일이라 미변경).
- 로컬 디렉터리 이름 미변경(세션/메모리 경로 의존).

## 외부 검증

GitHub repo 개명 1건(사용자 명시 지시). 그 외 외부 변경 없음.

## 남은 작업·차단 입력·다음 READY 티켓

- 차단 입력: Rackline 도메인 선택·등록(사용자), `vercel login`/`railway login`(사용자), 배포 승인.
- 다음: GPU-066.b(위 입력 후), GPU-053(배포 구성·키 분리, v2 서비스 추가).

## GPU-066.b — 도메인·재배포 실행 (2026-09-14, 사용자 지시 "rackline.studioliq.com으로 할거임. 로그인 해놨으니 진행")

| 단계 | 결과 |
| --- | --- |
| 도메인 결정 | `rackline.studioliq.com`(웹) / `api-rackline.studioliq.com`(API). `studioliq.com` DNS는 GoDaddy(`ns23/24.domaincontrol.com`) → 레코드는 사용자가 추가(아래 표). 구 `hashcredit`/`api-hashcredit` CNAME은 아직 존재 |
| Vercel | 구 링크(`apps/web/.vercel` → 폐기 프로젝트 `ctc-hashcredit`) scratchpad 백업 후 삭제. `vercel project add rackline`(팀 `elouanics-projects`), repo-root `.vercel` 링크(gitignore). `vercel api PATCH /v9/projects/rackline`: framework `vite`, rootDirectory `apps/web`, build `npm run build`, output `dist`, install `npm ci`. env `VITE_API_URL=https://api-rackline.studioliq.com` (production+preview). 도메인 `rackline.studioliq.com` 추가·verified. `vercel deploy --prod --yes` 성공 → `https://rackline-6vd7ndlmy-elouanics-projects.vercel.app`, alias `rackline.studioliq.com`. `vercel git connect` → `inchyangv/rackline` 연결(production branch `main`). SSO 보호는 `all_except_custom_domains`(커스텀 도메인은 공개) |
| Railway | `railway init --name rackline`(워크스페이스 "Incheol Yang's Projects", project `3162fb6b-…`). `railway add --service rackline-api --repo inchyangv/rackline` → `repo not found`(Railway GitHub app이 개명된 repo를 못 봄) → repo 연결 없이 서비스 생성, 변수 설정(`API_PROFILE=production`, `HOST`, `ALLOWED_ORIGINS`(rackline 도메인+localhost), `BORROWER_MAPPING_MODE=claim`, `CLAIM_SECRET`(`openssl rand -hex 32`, 출력·저장 없음), `CLAIM_TTL_SECONDS`, `BITCOIN_RPC_URL`, `BTC_INDEXER_BASE_URL`, `EVM_RPC_URL`, `CHAIN_ID=102031`, `HASH_CREDIT_MANAGER/CHECKPOINT_MANAGER/BTC_SPV_VERIFIER`=DEPLOY.md 현행 주소). admin/private key 없음. `railway up --service rackline-api` #1 **FAILED**: `hashcredit-api`가 GPU-015의 로컬 패키지 `hashcredit-gpu>=0.1.0`에 의존하는데 root Dockerfile이 `offchain/api/`만 복사 |
| Dockerfile 수정 | root `Dockerfile`/`Dockerfile.prover`: `offchain/gpu/`를 먼저 `pip install`, 그 다음 api/prover 설치. `docker-compose.yml`/`railway-compose.yml`: api/prover build context를 repo root로 변경. 로컬 `docker build` 성공, `--entrypoint python -c "import hashcredit_gpu, hashcredit_api.main"` ok, production profile `/health` → `{"status":"ok","api_profile":"production",…}` |
| Railway 재배포 | `railway up` #2 → 결과는 아래 "외부 검증"에 기록 |
| 커스텀 도메인 | `railway domain api-rackline.studioliq.com --service rackline-api` 추가 → CNAME `api-rackline` → `820l5mzl.up.railway.app`, TXT `_railway-verify.api-rackline` = `railway-verify=a8aeb3d9…a70f1` |

DNS(GoDaddy, zone `studioliq.com`) — 사용자 작업:

| Type | Name | Value |
| --- | --- | --- |
| A | `rackline` | `76.76.21.21` |
| CNAME | `api-rackline` | `820l5mzl.up.railway.app` |
| TXT | `_railway-verify.api-rackline` | `railway-verify=a8aeb3d9dc65dbe7a56cb87e319da04b1fce041c19683566da5b3e7d592a70f1` |

미완: Railway GitHub 자동 배포(UI에서 repo 연결 필요; 그 전까지 `railway up`), DNS 전파 후 `https://rackline.studioliq.com` / `https://api-rackline.studioliq.com/health` 스모크, README Status에 live URL 기재, `offchain/api/.env.compose`의 오래된 컨트랙트 주소(0x3cfb…) 정리(GPU-060), `apps/web/.vercel` 대신 repo-root `.vercel` 사용을 팀에 안내.

## Review

(독립 리뷰 세션이 작성)
