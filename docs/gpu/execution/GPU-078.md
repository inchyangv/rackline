# GPU-078 — 공식 AttestcoinRevenueVerifier·native 검증·엄격한 event 추출

```text
Ticket: GPU-078 / 상태: DONE (LOCAL) / 실행자: Claude Code (Opus 5, 1M) / 기준 commit: 0516783 + 작업 트리
증거 수준: LOCAL — BlockProver는 test double(MockBlockProver/ScriptedProver). native 통과 주장 없음.
executionProfile: LOCAL_MOCK (테스트) / verificationMethod: LOCAL_MOCK / nativeStatus: NOT_REQUESTED (G-ASC = GPU-080)
earningsProvenance: n/a (검증기는 발생 사실만 반환) / controlGrade: n/a / cashState: n/a
공식 통합: manifestHash sha256:2b0162d0…f6bb (cc3-testnet.sepolia.v1) 바인딩 값으로만 사용; @gluwa/usc-sdk 0.18.0 (encoding V1),
  @gluwa/asc-contracts 0.2.1 vendored EvmV1Decoder(2de1a8fa…)·full INativeQueryVerifier(bc81982e…);
  source Sepolia chainId 11155111 / chainKey 1; 검증 tx/proof artifact 없음(로컬 합성 fixture)
```

## 바뀐 동작과 파일

| 파일 | 변경 |
| --- | --- |
| `contracts/gpu/AttestcoinRevenueVerifier.sol` (신규) | 공식 `INativeQueryVerifier.verifyAndEmit`(단건, state-changing) 저수준 호출 → 반환이 정확히 1 word == 1일 때만 성공, revert/false/empty/짧은·긴·비-bool 반환 모두 `NativeVerificationFailed(reason)`; `calculateTxIndex` staticcall(32B·uint64 범위 검사); 검증된 bytes만 vendored `EvmV1Decoder`로 decode(외부 self-call try/catch → `MalformedEncoding(reason)`, tx type>4 → `TransactionTypeUnsupported`); receiptStatus==1, MAX_LOGS, (emitter, topic0) 매칭 → `VerifiedSourceEvent[]` (logOrdinal = receipt 내 index, id = GPU-076 §3 키); 생성자에서 profile↔precompile 주소(native는 0x…0FD2만, LOCAL_MOCK은 공식 주소 금지), encoding 1, limits>0, **registry provider의 sourceChain·profile과 동일 바인딩** 강제; 무상태·소비 없음(GPU-031 몫) |
| `contracts/gpu/interfaces/IRevenueVerifier.sol` | 오류 2개 추가: `MalformedEncoding(bytes)`, `ProviderNotAdmitted(ProviderId)` |
| `test/gpu/GPU078AttestcoinVerifierTest.t.sol` (신규) | 41 tests + `ScriptedProver` test double |
| `test/fixtures/gpu/attestcoin/wire/synthetic-obligation-v1.json` (신규, 기존 미추적 초안 채택) | SYNTHETIC wire fixture 5 case; 공식 SDK 인코더 출력 |
| `offchain/attestcoin/src/wire-fixtures.ts`, `src/cli.ts`(`gen-wire [--check]`), `package.json`, `test/wire-fixtures.test.ts` (신규/수정) | fixture를 pinned `@gluwa/usc-sdk@0.18.0 encoding.abiEncode(V1)`로 결정적 재생성; 파일과 바이트 동일성·envelope shape·금액/ref를 vitest로 고정 |
| `test/fixtures/gpu/abi/IRevenueVerifier.json`(갱신), `AttestcoinRevenueVerifier.json`(신규) | `forge inspect` ABI fixture (총 18) |
| `offchain/gpu/tests/test_solidity_abi_fixture.py` | 18개 기대, verifier 오류 surface·구현이 인터페이스 전체를 포함·서명/관리자/SPV fallback entry point 부재 검사 |

미추적 초안 `contracts/gpu/AttestcoinRevenueVerifier.sol`은 이전 세션 산출물로 확인 후 재작성했다(고수준 try/catch → 저수준 호출, 생성자 registry 바인딩, malformed 처리, `ManifestMismatch` actual 값). `docs/hackathon/*_EMAIL.md`, `MAIL.md`는 미변경·미stage.

## 적용한 결정 ID·버전 / 가정 / OPEN 항목

- 적용: R2-D01/D02(공식 native 필수), R2-D03(서명/관리자/SPV/mock fallback 금지 — 코드에 대체 경로 없음), R2-D05(성공 = source 사건 발생만), R2-D12(LOCAL mock ≠ native); GPU-076 §2–§3(proof-bound 위치·`sourceEventId`·worker hint 불신뢰); GPU-075 environment.md E2(full interface import).
- 가정: `verifyAndEmit` 단건 overload를 사용(batch overload는 범위 밖). Creditcoin 런타임의 실제 반환 encoding이 문서/ABI(bool)와 같다는 점은 GPU-080에서 실증. london EVM 유지.
- 설계 메모: `ManifestMismatch`는 생성자 바인딩 검사 때문에 현 registry 구현에서는 도달 불가(방어적 유지). `verifyAndExtract`는 공개 호출 가능하며 무상태 — 소비/dedup/업무 검증은 EvidenceBook. indexer는 `SourceEventVerified`가 아닌 EvidenceBook 이벤트를 기준으로 해야 한다(GPU-073 메모).
- OPEN: 없음(이 티켓). 실제 tamper 거부·precompile gas 상한 실측은 GPU-080/082.

## 완료 기준별 확인 결과

| 완료 기준 | 결과 |
| --- | --- |
| manifest/등록 source와 일치하는 proof만 공식 precompile에 검증 요청 | [통과] `test_reject_wrongChainKey`, `test_reject_admissionSwitchedOff`, 생성자 `provider source chain`/`provider profile`/unknown provider 거부 |
| 성공한 정확한 txBytes에서만 receipt/log 추출 | [통과] decode는 `_nativeVerify` 성공 후 동일 calldata bytes로만 수행; RPC receipt 입력 경로 없음 |
| false/revert/empty·malformed 응답 fail closed | [통과] `test_reject_nativeFalse`, `_nativeRevert_reasonForwarded`, `_nativeEmptyReturn`, `_nativeNonBooleanOrShortReturn`(2, 1B, 64B), `_nativeScriptedRevert`, `_txIndexOutOfRangeOrMalformed` |
| receiptStatus=1, 허용 tx type/encoding, emitter, topic, data 검사 | [통과] `_receiptStatusZero`, `_unsupportedTransactionType`(type 5), encoding≠1 생성자 거부, `_foreignEmitterWithIdenticalTopics`, `_requestingUnregisteredEmitter`, `_unregisteredTopic`, `_revokedEmitter`, `_emptyTopicList`; data 필드는 fixture 금액/ref/issuer와 대조(`_decodedDataMatchesFixtureAmountsAndRefs`) |
| worker가 보낸 amount/recipient/timestamp/txIndex를 proven 값으로 쓰지 않음 | [통과] envelope에 해당 필드 없음; txIndex는 `calculateTxIndex` 결과(`_twoLogs_…_withProvenPosition`, siblings isLeft 비트 = 17); height/txIndex 변경 시 id 변경(`test_positionBinding_…`) |
| log 선택 범위·proof bytes·siblings/continuity 길이·gas 상한 명세 | [통과] Limits{maxTxBytes,maxLogs,maxSiblings,maxContinuityRoots} 생성자 고정; `_oversizedEncodedTransaction`, `_oversizedSiblingsAndContinuity`, `_tooManyLogs`. gas 실측은 GPU-080 |
| native 실패/미지원/manifest mismatch에 서명 fallback 없음 | [통과] 코드에 서명/관리자 경로 없음; ABI fixture 검사(`test_interface_abis_…`)로 fallback entry point 부재 고정 |
| 검증 함수가 소비 권리를 선점하지 않음 | [통과] 무상태; `test_frontRun_publicVerifyHasNoStateEffect_sameResultForApp` |
| official success = source event 유효성일 뿐 | [통과] NatSpec·반환 타입에 매출/현금/권리 필드 없음 |
| LOCAL mock 주소/etch를 native/prod manifest로 사용 불가 | [통과] `_nativeProfileCannotBindMock`(NATIVE_TESTNET/PRODUCTION), `_localMockCannotClaimOfficialAddress`, `test_nativeProfile_bindsOfficialAddressOnly_…`(로컬 EVM에서 0xFD2 호출은 fail closed) |
| 검증 목록: 공식 wire fixture positive, receipt 실패, wrong chainKey/encoding/emitter/token/recipient, 금액·ordinal·proof 변조, oversized/malformed, native false/revert/empty, 다중 log, front-run | [통과] 위 항목 + `_mixedEmitters_onlyRegisteredEmitterAtOrdinal1`, `_multipleTopicsAllowed…`, `_malformedBytes_officialDecoderRevertIsWrapped`, `_truncatedFixtureBytes`, `_emptyEncodedTransaction`, `_tamperedAmountByte_whenNativeSaysNo`(실제 변조 거부는 precompile 몫 → GPU-080). token/recipient 귀속은 verifier가 아닌 EvidenceBook/GPU-031의 업무 검증 범위(verifier는 emitter/topic/receipt만) |
| 공식 wire fixture 출처 | [통과] `gen-wire --check` REPRODUCIBLE(바이트 동일), `wire-fixtures.test.ts` 3 tests; SYNTHETIC 표기, capture 아님 |

## 실행 명령·cwd·exit code·test 개수·중요 결과

| # | cwd | 명령 | exit | 결과 |
| --- | --- | --- | --- | --- |
| 1 | offchain/attestcoin | `npx tsx <scratch>/gen.ts > regen.json; cmp fixture regen.json` | 0 | IDENTICAL (bytes) — 초안 fixture의 SDK 출처 확인 |
| 2 | root | `npm --prefix offchain/attestcoin run check` (ASC-CHECK) | 0 | artifacts OK, ABI OK, 2 manifests VALID |
| 3 | root | `npm --prefix offchain/attestcoin run test -- --run` (ASC-TEST) | 0 | 6 files, **39 passed** (wire-fixtures 3 포함) |
| 4 | root | `npm --prefix offchain/attestcoin run gen-wire -- --check` | 0 | REPRODUCIBLE |
| 5 | root | `forge test --match-contract 'GPU078AttestcoinVerifierTest' -vvv` (SOL) | 0 | **41 passed / 0 failed** |
| 6 | root | `forge inspect IRevenueVerifier abi --json`, `forge inspect AttestcoinRevenueVerifier abi --json` | 0 | fixtures |
| 7 | root | `.venv-py313/bin/python -m pytest offchain/gpu/tests/test_solidity_abi_fixture.py -q` | 0 | 3 passed (PG 불필요 부분만) |
| 8 | root | `forge build --sizes` | 0 | AttestcoinRevenueVerifier runtime 10,019 B |
| 9 | root | `forge test --summary` (SOL-FULL) | 0 | 15 suites, **267 passed / 0 failed / 0 skipped** (226 기존 + 41) |
| 10 | root | `forge fmt --check` | 1 | 기준선 `contracts/BtcSpvVerifier.sol` 1건만(GPU-000 baseline); 신규/수정 파일은 fmt 적용 |
| 11 | root | `git diff --check` | 0 | clean |

## 실패/미실행과 이유

- 실제 native 검증(NATIVE_TESTNET) 미실행 — 승인·자금·실 proof 없음; GPU-080 범위. 이 티켓의 모든 positive는 mock 검증.
- PY-GPU 전체(PostgreSQL 필요) 미실행 — 이 티켓은 DB 스키마를 건드리지 않았고 ABI fixture test만 관련.
- `forge fmt --check` 기준선 실패 유지(레거시 파일; 무관한 정리 금지).
- 미검증: Creditcoin 런타임의 precompile gas 소모·batch overload·verifyAndEmit 이벤트 encoding 실제 값(GPU-080/082).

## 외부 검증

없음. RPC 쓰기·broadcast·자금 이동·파트너 접촉 없음.

## 남은 작업·차단 입력·다음 READY 티켓

- 차단 입력: 없음(LOCAL 완료). native 실증은 GPU-080(승인된 testnet·지갑·가스 예산 필요 → 현재 BLOCKED_EXTERNAL 예정).
- 다음 READY: **GPU-077**(source 모듈·MockDePINPayout; 선행 029/030/076 완료), **GPU-031**(EvidenceBook; 선행 확인 필요), GPU-032, GPU-033, GPU-016, GPU-017/018, GPU-060. GPU-079/080/082는 078 완료로 한 단계 진전.

## 재개 시 필요한 파일·실행 중 프로세스/포트·변경 소유권

- 파일: 이 기록, `contracts/gpu/AttestcoinRevenueVerifier.sol`, `test/gpu/GPU078AttestcoinVerifierTest.t.sol`, `offchain/attestcoin/src/wire-fixtures.ts`, `docs/gpu/attestcoin/evidence-contract.md` §3.
- 실행 중 프로세스/포트: 없음.
- 변경 소유권: `IRevenueVerifier` 오류 추가는 GPU-029 인터페이스 소유권 하에 이 세션이 갱신(ABI fixture 동기화 완료).

## Review

(독립 리뷰 세션이 작성)
