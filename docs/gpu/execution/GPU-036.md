# GPU-036 — facility 승인·신규 draw의 원자적 실행

```text
Ticket: GPU-036 / 상태: DONE (LOCAL) / 실행자: Claude Code fork (Opus 5, 1M) / 기준 commit: abb27e5 + 작업 트리
증거 수준: LOCAL — MockBlockProver + SYNTHETIC wire fixture; 실제 native 통과 없음(G-ASC = GPU-080)
executionProfile: LOCAL_MOCK (테스트) / verificationMethod: LOCAL_MOCK / nativeStatus: CONSUMED(mock) → 적격 base
earningsProvenance: SIMULATED (mock provider) / controlGrade: E2 (registry 기록·servicer 관측; 실제 upstream lock 아님) / cashState: 없음
공식 통합: GPU-078/031 경로 그대로 (manifest sha256:2b0162d0…f6bb, usc-sdk 0.18.0, asc-contracts 0.2.1); proof artifact 없음
출시 조건: 로컬 fixture의 control/정책은 테스트 전용. 일반 고객/LP funded draw 활성화는 GPU-010/063 승인 이후; 소액 시험 대출은 GPU-056 §0.4 규칙.
```

## 바뀐 동작과 파일

| 파일 | 변경 |
| --- | --- |
| `contracts/gpu/CreditFacilityManager.sol` (신규) | `openFacility`(UNDERWRITER: 지갑 링크·ReceivableBook facility·control agreement 차주 일치·verifier profile 일치 검사 → ledger `open`, DRAFT), `transition`(GPU-013 표의 권한/guard: registrar→UNDER_REVIEW, underwriter→CONTROL_PENDING(anchor 유효 필요), underwriter/servicer→ACTIVE(`requireUsable(E2)`), guardian→DRAW_FROZEN, underwriter→ACTIVE(cause_cleared), ADMIN(=credit_committee)→DEFAULTED, servicer→RECOVERY, ADMIN/treasury→CLOSED_WITH_LOSS, servicer/treasury→RELEASED(debt==0), system(자기 자신)→REPAID/DELINQUENT), `freezeDraws`(GUARDIAN), `pauseDraws`(GUARDIAN, 상환 무관), `anchorAuthorization`(underwriter CREDIT_APPROVAL 서명을 `keccak256(abi.encode(CreditAuthorization))` subject로 AuthorizationVerifier에서 검증·nonce 소비; validUntil>now, policy current, manifest == provider verifier manifest, controlAgreementVersionHash == facility의 (agreementId, version), **ExposureController authorization과 limit/policy/control/validUntil 일치** 필수; epoch 증가로 상위 anchor 교체), `borrow`(링크된 지갑만; pause·ACTIVE·anchor 유효·manifest·policy·control(E2 usable)·`evaluateDraw` eligible>0 & evidence 미만료(`NativeEvidenceRequired`)·amount≤availableDraw·vault cash → 한 tx에서 reserve→consume(ledger recordDraw)→`vault.lend`(지갑 고정)→수령 delta≥minReceived), `reserveDraw`/`executeReservedDraw`(2단계; 만료·중복·권한 변경은 ExposureController가 거부), `repayFor`(누구나·언제나; caller→vault 실측 transfer→`ledger.allocate`→`vault.onRepayment`; excess는 차주 refundable; debt==0 ⇒ REPAID), `claimRefund`(링크 지갑만), views. 수취인 파라미터·한도 증액·testnet grant·pauseRepayments 없음 |
| `test/gpu/GPU036DrawTest.t.sol` (신규) | 27 tests + `ShortfallToken` TEST_ONLY 대출 토큰(부분 전달 시뮬레이션) |
| `test/fixtures/gpu/facility-transitions-v1.json` (신규) | `config/gpu/schema/facility-transitions-v1.json` 바이트 동일 사본(`cmp` 확인; foundry `fs_permissions`가 `./test/fixtures`만 허용) — Solidity 전이표 parity 테스트 입력 |

인터페이스 변경 없음(`ICreditFacilityManager` 그대로). 선행 컨트랙트 미수정: 부족한 표면은 로컬 최소 인터페이스로 접근(`DebtLedger.setState`, `ControlRegistry.requireUsable`, `LendingVaultV2.refundExcess/refundableOf`).

## 적용한 결정 ID·버전 / 가정 / OPEN 항목

- 적용: R2-D02(적격 base는 EvidenceBook→ReceivableBook의 native(mock) 소비 기록만), R2-D03(서명만으로 draw 불가 — `test_noNativeEvidence_signatureAloneCannotDraw`), R2-D04(underwriter 서명 = 보조 승인), R2-D05, R2-D07/PD-03(상환은 실측 destination 수령·allocate), PD-06(pause≠repay), PD-08(grant/owner-key 경로 없음), GPU-013 §4 전이표, GPU-035 reservation 계약.
- 설계 결정: (a) ExposureController `setAuthorization`이 UNDERWRITER 전용이라 manager가 대신 쓸 수 없음 → **두 기록의 일치**를 anchor 조건으로 요구(둘 다 underwriter 행위; 어느 한쪽만으로 한도 불가). (b) `credit_committee` = ADMIN_ROLE(임시, GPU-043). (c) `system` 전이 중 계산 가능한 REPAID만 자동(repayFor); DELINQUENT/cure/evidence-stale freeze 자동화는 GPU-041/044. (d) 2단계 예약은 borrower 지갑만 호출 가능; 내부 nonce 단조 증가.
- 가정: 대출 토큰과 source 토큰이 6 decimals 1:1(GPU-035 pilot valuation 제한 그대로).
- OPEN: 없음(이 티켓). GPU-041에서 DELINQUENT/DEFAULT 스케줄, GPU-043에서 committee 역할·timelock.

## 완료 기준별 확인 결과

| 완료 기준 | 결과 |
| --- | --- |
| borrower/facility/vault/asset/termsHash/policyVersion/controlVersion/nonce/expiry에 승인 귀속 | [통과] `CreditAuthorization` subject 서명 + AuthorizationVerifier nonce/expiry/epoch + facility의 control version hash + verifier manifest + ExposureController 일치 (`test_wrongFacilityAuthorization_rejected`, `test_manifestMismatch_rejected`, `test_controlVersionBumpedAfterAnchor_rejected`, `test_duplicateApprovalNonce_rejected`, `test_expiredAnchor_cannotBeAnchored`) |
| 최신 debt·미납·유효 통제·현재 headroom·현금 검증 후 예약 consume/채무 증가/실송금 원자 처리 | [통과] `test_happyPath_…`(reserved 0, principal 5,000, cash −5,000 같은 tx), `test_capRace_…`(accrued debt 포함 headroom), `test_insufficientVaultLiquidity`, `test_controlObservationStale_rejected` |
| approve된 수취인 외 자금 이동 불가 | [통과] `borrow`에 수취인 파라미터 없음; `lend(…, f.wallet, …)` 고정; `test_noTestnetGrantOrLimitIncreaseSelectors`(`borrowTo` 등 selector 부재) |
| token transfer 실패 시 전부 rollback | [통과] `test_partialTokenDelivery_rollsBackEverything`(principal 0, reserved 0, cash 불변) |
| control/policy 변경 후 오래된 승인 사용 불가 | [통과] `test_controlVersionBumpedAfterAnchor_rejected`, `test_policyVersionChanged_rejected` |
| 계약 약정 없이 자동 갱신/증액 없음 | [통과] `test_lateMinedExpiredApproval_rejected`, `test_newAnchorSupersedes_lowerLimitNeverErasesDebt`(한도 하락은 debt 불변, 신규 draw 0) |
| 검증 목록: front-run, 중복 nonce·예약, cap race, 멈춘 draw, 유동성 부족, wrong vault/asset/borrower, late-mined expired approval, 부분 token 실패 | [통과] `test_frontRun_…`, `test_duplicateApprovalNonce_…`, `test_duplicateReservation_…`, `test_capRace_…`, `test_stalledDraw_…`, `test_insufficientVaultLiquidity`, `test_wrongFacilityAuthorization_…`/`test_wrongBorrowerWallet_…`/`test_profileMismatch_…`, `test_lateMined…`, `test_partialTokenDelivery_…` |
| R2: 최종 온체인 draw에 native requirement; API 검사/심사 signature만으로 실행 불가; 증거 누락/만료/version mismatch 거부 | [통과] `test_noNativeEvidence_signatureAloneCannotDraw`, `test_expiredEvidence_rejected`, manifest/policy/control mismatch 테스트 |
| GPU-013 전이표 parity | [통과] `test_transitionTable_matchesGpu013Json`(JSON 16 transitions ↔ `isTransitionAllowed` 전 조합), `test_transitions_authoritiesAndGuards`, `test_releaseRequiresZeroDebt_andRepaidIsSystemOnly` |
| pause는 draw만, repayFor는 유지 | [통과] `test_pausedDraws_blockBorrow_notRepayFor`(글로벌 pause + facility freeze) |
| 제3자 상환·excess 소유권 | [통과] `test_repayFor_byThirdParty_excessRefundableToBorrowerOnly`(이자 500 LP 수익, excess 500 차주 refundable, mallory claim 불가) |

## 실행 명령·cwd·exit code·test 개수·중요 결과

| # | cwd | 명령 | exit | 결과 |
| --- | --- | --- | --- | --- |
| 1 | root | `FOUNDRY_OUT=out-gpu036 FOUNDRY_CACHE_PATH=cache-gpu036 forge build` | 0 | lint 경고만(unchecked cast/modifier 스타일, 기존 GPU 컨트랙트와 동일) |
| 2 | root | `FOUNDRY_OUT=out-gpu036 FOUNDRY_CACHE_PATH=cache-gpu036 forge test --match-contract 'GPU036DrawTest' -vvv` | 0 | **27 passed / 0 failed / 0 skipped** |
| 3 | root | `… forge test --match-contract 'GPU036DrawTest\|GPU035RiskTest\|GPU034VaultTest'` (fmt 후 회귀) | 0 | 27 + 31 + 20 passed |
| 4 | root | `forge fmt contracts/gpu/CreditFacilityManager.sol test/gpu/GPU036DrawTest.t.sol` | 0 | 적용 |
| 5 | root | `git diff --check`; `cmp config/gpu/schema/facility-transitions-v1.json test/fixtures/gpu/facility-transitions-v1.json` | 0 / 0 | clean / identical |

## 실패/미실행과 이유

- SOL-FULL·ABI fixture export·TICKET.md 갱신은 통합 담당(부모 세션) 수행.
- 실제 native 검증·실자금 draw 미실행(GPU-080/056/063). 실제 upstream lock 없음(E2는 registry 기록 + mock 관측).
- DELINQUENT/cure/evidence-stale 자동 전이 미구현(GPU-041/044); committee 역할은 ADMIN 대체(GPU-043).

## 외부 검증

없음.

## 남은 작업·차단 입력·다음 READY 티켓

- 차단 입력: 없음(LOCAL).
- 다음 READY: GPU-039(repayFor router — 이 manager의 `repayFor`를 router로 분리/확장), GPU-041(연체·default·reserve; `system` 전이 자동화), GPU-043(wiring/timelock/committee). GPU-037 완료 후 GPU-038/040.

## 재개 시 필요한 파일·실행 중 프로세스/포트·변경 소유권

- 파일: 이 기록, `contracts/gpu/CreditFacilityManager.sol`, `test/gpu/GPU036DrawTest.t.sol`, `test/fixtures/gpu/facility-transitions-v1.json`.
- 프로세스/포트: 없음. 임시 `out-gpu036`/`cache-gpu036` 삭제됨.
- 소유권: ABI fixture(`ICreditFacilityManager`, `CreditFacilityManager`)·count 갱신·TICKET 상태 = 통합 담당.

## Review

(독립 리뷰 세션이 작성)
