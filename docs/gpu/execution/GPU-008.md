# GPU-008 — Creditcoin·공식 source 적합성·자산·환전·결제 레일 ADR

```text
Ticket: GPU-008 / 상태: DONE (DRAFT v0.1 + OPEN SR-O01~O06) / 실행자: Claude Code (Opus 5, 1M) / 기준 commit: ca1ac8c + 작업 트리
증거 수준: LOCAL (SPEC; 문서·schema·validator만. 자금 이동·RPC 쓰기·파트너 연락 없음)
executionProfile: n/a / verificationMethod: n/a / nativeStatus: n/a / cashState: 경로 정의만 (§3 표)
공식 통합: Attestcoin manifest cc3-testnet.sepolia.v1 (sha256:2b0162d0…f6bb)를 rail의 facts 레일 참조로 사용; 지원 source = 문서+probe 값(runtime proof 미검증)
```

## 바뀐 동작과 파일

| 파일 | 변경 |
| --- | --- |
| `docs/gpu/decisions/settlement-rails.md` (신규) | ADR DRAFT v0.1: SR-D01~D08 확정 baseline(Creditcoin 원장, 공식 native 검증, 사실↔자금 레일 분리, source escrow, UNSUPPORTED_SOURCE=disabled, 대안 구성 미채택, Writability 미포함, settlementId 연결), 공식 지원표 vs 파트너 현실(Aethir/GPU.net 모두 UNCONFIRMED→DISABLED), 한 settlementId 자금 경로 6-leg 표(debt는 leg 5 `repayFor`에서만 변동), 신뢰 주체 표, 자산/환전/bridge/수수료/복구 규칙(값은 UNCONFIRMED), OPEN SR-O01~O06, NO_GO 조건 |
| `config/gpu/schema/settlement-rails-v1.schema.json` (신규) | rail manifest schema: debt chain(Creditcoin id·RPC/explorer·finalized), source chain(EVM id·chainKey·encoding 1·attestation genesis·finality=attestation·supportStatus), assets(source/loan token: address|null, decimals, issuerRef, status), escrow(address/deploymentTx/codeHash|null), conversion/settlement leg(status, counterparty, contract, feeModel, failureOwner, recoveryProcedure, limits), railStatus, partnerSourceBinding, evidence(Attestcoin manifest id/hash, requiredVerification=ATTESTCOIN_NATIVE) |
| `config/gpu/rails/cc3-testnet.mockdepin.draft.json` (신규) | TEST_ONLY 인스턴스(Sepolia→CC3 testnet, mockdepin, 모든 주소 null, railStatus=TEST_ONLY, binding=UNCONFIGURED) |
| `offchain/attestcoin/src/rails.ts` (신규), `src/cli.ts`(check에 rail 검증 추가), `test/rails.test.ts` (신규, 7 tests) | 구조 검증 + Attestcoin manifest 교차검증(id 존재·hash 일치·profile/destination 일치·source chainKey/chainId/encoding 지원표 일치 → 아니면 UNSUPPORTED_SOURCE), PRODUCTION 규칙(UNCONFIRMED/null/TEST_ONLY 금지, binding=VERIFIED 필요, non-mock PROBED manifest), 테스트 profile에서 VERIFIED/PILOT_APPROVED 주장 금지 |
| `docs/gpu/decisions/REGISTER.md` | settlement-rails.md 색인, SR-O01~O06 항목 |

## 적용한 결정 ID·버전 / 가정 / OPEN 항목

- 적용: R2-D01/D02/D07/D08/D09/D10, PD-01/03/06, GPU-075 environment.md §4(지원표·probe), GPU-076 §4/§5, GPU-077 SourceEscrow(settlementId·measured delta).
- 가정: 파트너 자료 없음(GPU-004/005 미착수) → 파트너 rail은 전부 UNCONFIRMED/DISABLED. CC3 mainnet RPC 미확인 → production rail 인스턴스 없음.
- OPEN: SR-O01(대출 stablecoin=R2-O06/TS-O02), SR-O02(파트너 실제 contract/chain=R2-O01/O02), SR-O03(bridge/settlement 파트너=R2-O05), SR-O04(환전 venue/한도), SR-O05(API-only 시 admission 보류 vs 상품 변경=R2-O03), SR-O06(대안 vault chain=R2-O05).

## 완료 기준별 확인 결과

| 완료 기준 | 결과 |
| --- | --- |
| read proof와 자금 이동 구분; 미지원 chain/자산 disabled | [통과] SR-D03 + §3 표(leg 0–1 proof, 2–5 money); validator UNSUPPORTED_SOURCE·DISABLED 규칙(`rails.test.ts` Arbitrum 케이스) |
| 확정채권 사건·지급 사건·destination 자금 경로·신뢰 주체 구분 | [통과] §3 leg 표 + §4 신뢰 주체 표 |
| 자체 서명/source-native 대출 자동 전환 없음; 양립 불가 시 OPEN/NO_GO | [통과] SR-D05/D06, §7 SR-O05, §8 NO_GO |
| 기술 기준선만으로 LIVE 준비 완료 금지 | [통과] DRAFT 표기, PRODUCTION rail 생성 조건(SR-O 해소 + GPU-062 probe), validator가 PRODUCTION+UNCONFIRMED 거부 |
| 검증: 한 settlement ID로 source receipt 이후 debt/NAV 변경 시점 명세 | [통과] §3: receivable `unpaid`는 leg 1, facility debt는 leg 5에서만; in-flight는 현금/NAV 아님; §9에서 구현 코드(SourceEscrow→Verifier→EvidenceBook→ReceivableBook vs DebtLedger.allocate)와 대조 |
| manifest schema(source/debt chain·token/decimals·escrow·RPC·explorer·finality) | [통과] schema + TEST_ONLY 인스턴스 + ASC-CHECK 통합 |

## 실행 명령·cwd·exit code·test 개수·중요 결과

| # | cwd | 명령 | exit | 결과 |
| --- | --- | --- | --- | --- |
| 1 | root | `npm --prefix offchain/attestcoin run check` (ASC-CHECK) | 0 | artifacts OK, 2 manifests VALID, `rail cc3-testnet.mockdepin.draft.json: VALID profile=NATIVE_TESTNET status=TEST_ONLY binding=UNCONFIGURED` |
| 2 | root | `npm --prefix offchain/attestcoin run test -- --run` (ASC-TEST) | 0 | 7 files, **46 passed** (rails 7 포함) |
| 3 | root | `git diff --check` | 0 | clean |

## 실패/미실행과 이유

- 실제 자금 경로 실행·bridge/환전 실증 없음(GPU-009/040/056 권한). 파트너 rail 값은 자료 부재로 UNCONFIRMED.
- JSON Schema를 외부 validator(ajv 등)로 실행하지 않음 — 의존성 추가 대신 `rails.ts`의 명시 검사 + schema key 정합 테스트로 대체(의존성 추가는 GPU-053/054에서 결정).

## 외부 검증

없음.

## 남은 작업·차단 입력·다음 READY 티켓

- 차단 입력: SR-O01~O04 (issuer, 파트너 contract/chain, bridge 파트너, 환전 정책) — 사용자/파트너 결정.
- 다음 READY: GPU-036(선행 032/033/034/035 — 035 진행 중), GPU-037/039/040(040은 이 ADR의 rail 규칙을 구현), GPU-017/019, GPU-060.

## 재개 시 필요한 파일·실행 중 프로세스/포트·변경 소유권

- 파일: `docs/gpu/decisions/settlement-rails.md`, `config/gpu/schema/settlement-rails-v1.schema.json`, `config/gpu/rails/*.json`, `offchain/attestcoin/src/rails.ts`.
- 프로세스/포트: 없음. 소유권: rail schema/validator = offchain/attestcoin(이 세션).

## Review

(독립 리뷰 세션이 작성)
