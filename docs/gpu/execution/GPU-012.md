# GPU-012 — 금융 회계 명세와 독립 reference model

```text
Ticket: GPU-012 / 상태: DONE (SPEC v1 + reference model) / 실행자: Claude Code (Opus 5, 1M) / 기준 commit: 08066fe + 작업 트리
증거 수준: LOCAL (정수 reference model, 13 vectors + 2 invariant tests)
공식 통합: 없음
```

## 바뀐 동작과 파일

| 파일 | 변경 |
| --- | --- |
| `docs/gpu/accounting.md` (신규) | 원장 4종 분리(법적 채무 vs NAV), 이자(ACT/365, 정확 누산기, 세그먼트, 자본화 기본 OFF, 상각 후 비발생), waterfall, 손실(impair/write-off/recovery), LP share(virtual offset), NAV·이중계상 금지, 분개표, R2 케이스, 벡터 목록 |
| `offchain/gpu/hashcredit_gpu/accounting/{__init__,reference}.py` (신규) | `FacilityLedger`(numerator 누산·세그먼트·waterfall), `VaultLedger`(cash/NAV/shares/pipeline/reserve/impairment/write-off/recovery, clock), `borrowing_base` |
| `test/fixtures/gpu/accounting.json` (신규) | AC-01~AC-13, 기대값은 문서 공식에서 수기 유도 |
| `offchain/gpu/tests/test_accounting_reference.py` (신규) | 벡터 러너 + 전역 불변식 + evidence 비접촉·경계 테스트 |

## 적용한 결정 ID·버전 / 가정 / OPEN 항목

- 적용: R2-D06/D07/D12; PIVOT §2.2(AR-01~04/08 정정값), §5.2 waterfall, §7 NAV 규칙; term sheet §4(ACT/365 고정 APR, 자본화 OFF).
- 가정: 상각 후 계약상 default interest는 원장 밖(OPEN TS-O09). virtual offset 1000/1은 GPU-034에서 ERC-4626 채택 범위와 함께 확정.
- 개발 중 정정 3건(모델): NAV를 원장 clock 기준으로 평가(지연 발생 반영), 상각 시 발생 동결, offset을 OZ 방식(+1 asset)으로 — 벡터 기대값은 유지, AC-03은 시퀀스 오류 수정.

## 완료 기준별 확인 결과

| 완료 기준 | 결과 |
| --- | --- |
| 법적 채무와 NAV 장부가가 별개 | [통과] AC-08/09/13: write-off·impairment 후 legal_debt 불변, NAV 변동 |
| source/in-flight/destination 이중 계상 금지 분개 | [통과] AC-07 + §6/§7 |
| 차주 매출채권의 LP NAV 가산 금지 | [통과] NAV 정의(§6), `borrowing_base`는 원장 외부 |
| 환불 reserve의 LP 귀속 금지 | [통과] `borrower_reserve`/`borrower_refundable` off-NAV, AC-06/08 |
| rate 변경 시 정확한 구간별 정산 | [통과] AC-02 $1,500 |
| $5,000·10%·1년·$250 → 원금 $5,000/이자 $250 | [통과] AC-01 |
| 두 차주·초과입금·상각 후 회수·전액손실/새 LP 진입 | [통과] AC-05/06/08/09 |
| TEST_ONLY 약정과 승인 값 구분 | [통과] `Terms.test_only`; DB 트리거(GPU-015) |
| R2: 완납채권→base 0, proof만→debt/NAV 불변, native 장애 중 repayFor 정상 배분 | [통과] AC-11, `test_evidence_never_touches_the_ledger`, AC-12 |

## 실행 명령·cwd·exit code·test 개수·중요 결과

| # | cwd | 명령 | exit | 결과 |
| --- | --- | --- | --- | --- |
| 1 | root | `python -m pytest offchain/gpu/tests/test_accounting_reference.py -q` (초기) | 1 | 4 failed → 모델 정정 3건 + 벡터 순서 1건 |
| 2 | root | 동일 (최종) | 0 | **15 passed** |
| 3 | root | `python -m pytest offchain/gpu/tests -q` (PY-GPU 전체, PostgreSQL 포함) | 0 | **33 passed** |
| 4 | root | `git diff --check` | 0 | |

## 남은 작업·차단 입력·다음 READY 티켓

- 다음 READY: **GPU-076**(선행 011/012/013/075 충족), GPU-008(SPEC), GPU-017/018(CODE), GPU-060.

## Review

(독립 리뷰 세션이 작성)
