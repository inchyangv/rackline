# GPU-015 — 공통 Python package와 핵심 도메인 DB migration

```text
Ticket: GPU-015 / 상태: DONE / 실행자: Claude Code (Opus 5, 1M) / 기준 commit: 2dccd19 + 작업 트리
증거 수준: LOCAL (실제 PostgreSQL 16.13 임시 클러스터에서 migration/constraint/backup-restore 실행)
공식 통합: 없음 (providers.manifest_hash 참조 컬럼만)
```

## 바뀐 동작과 파일

| 파일 | 변경 |
| --- | --- |
| `offchain/gpu/pyproject.toml`, `README.md`, `alembic.ini` (신규) | package `hashcredit-gpu` 0.1.0: sqlalchemy/alembic/psycopg2/pydantic/structlog/jsonschema; CLI `hashcredit-gpu-db` |
| `offchain/gpu/hashcredit_gpu/domain/{enums,money}.py` | 14 enum(StrEnum)이 `domain-v1.schema.json`과 동일(테스트 단언); `AssetRef`/`Money`(정수 문자열, 자산 불일치 연산 거부) |
| `offchain/gpu/hashcredit_gpu/db/{models,settings,cli}.py` | 18 테이블 ORM 미러(FK/unique/partial unique/check 제약 40+), URL 해석(PostgreSQL 전용, 로그 시 redact), CLI upgrade/downgrade/current/check(드리프트 가드) |
| `offchain/gpu/migrations/{env.py,script.py.mako,versions/0001_core_domain.py}` | autogenerate로 1회 생성 후 동결한 DDL + PL/pgSQL 트리거 2개(decision↔facility profile 일치; PRODUCTION facility의 TEST_ONLY policy/terms·비-PRODUCTION provider 거부; execution_profile 불변) |
| `offchain/gpu/tests/{conftest,test_schema,test_migrations}.py` | 18 tests; conftest는 env URL 또는 로컬 initdb/pg_ctl 임시 클러스터(없으면 **fail**, skip 아님) |
| `offchain/api/pyproject.toml`, `offchain/prover/pyproject.toml` | `hashcredit-gpu>=0.1.0` 의존 추가 |
| `offchain/api/tests/test_gpu_package_import.py`, `offchain/prover/tests/test_gpu_package_import.py` | import smoke |
| `.github/workflows/test.yml` | postgres:16 service, gpu package 선설치, postgresql-client, PY-GPU 단계 |

## 적용한 결정 ID·버전 / 가정 / OPEN 항목

- 적용: R2-D02(required_verification 상수), R2-D12(profile 격리 트리거·불변), GPU-011 enum/ID/Money 계약, GPU-013 상태·권한(transition audit에서 oracle/keeper 거부, funded 상태의 agreement version 고정, E2 요건).
- 가정: receivables/evidence/cash/job 테이블은 GPU-016(asset_encumbrances.receivable_id FK는 그때 추가). 실제 provider 행은 등록 시 manifestHash와 함께 삽입.
- OPEN: 없음.

## 완료 기준별 확인 결과

| 완료 기준 | 결과 |
| --- | --- |
| 빈 PostgreSQL→head | [통과] `test_empty_database_to_head_and_back` (19 tables, 2 triggers, drift 0) |
| 이전 schema fixture→head | [통과] `test_previous_schema_fixture_to_head_preserves_legacy_table` (v1 `processed_payouts` 보존, downgrade 후에도 존재) |
| backup/restore의 ID·관계·합계 보존 | [통과] `test_backup_restore_preserves_ids_relations_and_sums` (pg_dump custom → downgrade+drop → pg_restore; facility id/원리금 합계/assignment 열림 상태/decision 동일; 트리거 생존) |
| `create_all()`로 production migration 대체 금지 | [통과] migration은 동결 DDL; CLI/env는 metadata를 비교에만 사용 |
| 민감 자료는 참조만 | [통과] `ck_legal_entities_refs_only`, `ck_provider_accounts_credential_ref`, `ck_*_doc_ref` + `test_secret_refs_only` |
| FK·unique·check constraints | [통과] `test_schema.py` 11 tests (wallet 단일 링크, provider account id 형식, 동일 serial 중복 자산 거부, 열린 assignment 1개, E2 요건, 음수 금액, ACTIVE에 agreement 필요, RELEASED에 debt 0, decision 권한/유효기간) |
| Python venv editable/dev 설치, 재현 migration CLI | [통과] `pip install -e offchain/gpu[dev]`; `hashcredit-gpu-db upgrade/check/downgrade` 실행 |
| PY-GPU(test_schema/test_migrations), PY-API/PY-PROVER import smoke | [통과] 18 / 39 / 62 passed |
| SQLite만 통과 금지 | [통과] settings가 PostgreSQL URL만 허용; 테스트는 PG 없으면 fail |
| R2: provider/facility/decision에 requiredVerification·policyVersion·source namespace·executionProfile 저장 | [통과] 컬럼 + 제약 |
| R2: native testnet 데이터의 production 재사용 거부(제약·migration 테스트) | [통과] `test_production_facility_rejects_test_only_policy_terms_and_testnet_provider`, `test_execution_profile_is_immutable`, `test_credit_decision_profile_must_match_facility`, backup/restore 후 트리거 재검증 |

## 실행 명령·cwd·exit code·test 개수·중요 결과

| # | cwd | 명령 | exit | 결과 |
| --- | --- | --- | --- | --- |
| 1 | scratch | `initdb --locale=C` + `pg_ctl start -o "-p 54329 -c unix_socket_directories=''"` (PostgreSQL 16.13 Homebrew) | 0 | 수동 클러스터(생성 후 종료) |
| 2 | root | `pip install -e "offchain/gpu[dev]"` | 0 | alembic 1.20.0 |
| 3 | offchain/gpu | alembic `revision --autogenerate` (1회, 동결) | 0 | 18 create_table |
| 4 | offchain/gpu | `hashcredit-gpu-db upgrade head` / `check` / `downgrade base` / `upgrade head` | 0/0/0/0 | 19 tables, no drift, base=1 table, 재적용 OK, 트리거 2 |
| 5 | root | `python -m pytest offchain/gpu/tests -q` | 0 | **18 passed** (임시 클러스터 자동 기동·정리) |
| 6 | offchain/api, prover, relayer | pytest | 0/0/0 | 39 / 62 / 9 passed |
| 7 | root | `pip install -e offchain/api[dev] -e offchain/prover[dev]` | 0 | 새 의존성 해석 |
| 8 | root | `git diff --check` | 0 | |

## 실패/미실행과 이유

- CI 워크플로 변경은 로컬에서 실행 불가(GitHub Actions) — 구성만 반영, GPU-054에서 검증.
- receivables/settlements/cash/jobs/audit 테이블은 범위 밖(GPU-016).

## 남은 작업·차단 입력·다음 READY 티켓

- 다음 READY: **GPU-016** (선행 확인 필요), GPU-017(선행 015). GPU-012는 GPU-007 대기.

## 재개 시 필요한 파일·실행 중 프로세스/포트·변경 소유권

- 파일: `offchain/gpu/**`. 실행 중 프로세스: 없음(임시 클러스터 종료·삭제 확인).
- schema 변경은 새 migration 파일 + `hashcredit-gpu-db check` 0 drift 필수; 0001은 동결.

## Review

(독립 리뷰 세션이 작성)
