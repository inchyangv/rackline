# GPU-074 — R2 결정 원장·현재 구현 inventory·문서 충돌 정리

```text
Ticket: GPU-074 / 상태: DONE / 실행자: Claude Code (Opus 5, 1M) / 기준 commit: aea2405 (main) + 작업 트리
dirty 범위: 기존 미추적 docs/hackathon/{CEIP_EMAIL,FOLLOWUP_EMAIL,MAIL}.md 보존(미변경, 미stage)
증거 수준: LOCAL (문서·저장소 grep 기반; 체인/RPC/파트너 조회 없음)
executionProfile: n/a / verificationMethod: n/a / nativeStatus: NOT_IMPLEMENTED
earningsProvenance: n/a / controlGrade: n/a / cashState: n/a
공식 통합: 없음 — SDK/ABI/decoder/endpoint 명칭은 UNCONFIRMED로 등록(C11), GPU-075가 고정
```

## 바뀐 동작과 파일

| 파일 | 변경 |
| --- | --- |
| `docs/gpu/decisions/attestcoin-first.md` (신규) | R2 결정 원장 v1: FIXED R2-D01~D14, 해석 규칙 4개, OPEN R2-O01~O09(사용자/사업 결정 필요) |
| `docs/gpu/execution/ATTESTCOIN_GAP.md` (신규) | 구현 inventory(13행, 전부 PLANNED/legacy/UNVERIFIED_EXTERNAL), 문서 충돌 등록부 C01~C20 + 담당 GPU ID, 문서↔코드 교차표 |
| `README.md` | 13개 문구 정합화: Status 표 v2 행 planned, 피벗 공지에 결정 원장 링크, "Fallback … lower advance rate" → `UNSUPPORTED_SOURCE`/무대체, 재사용 표 relayer legacy, "run as real contracts" → designed/미구현, 프로젝트 구조의 `contracts/gpu`·`test/gpu`·`keeper` planned |
| `TECH.md` | 6개: 헤더 R2 note, `RelayerSigVerifier` 행, `evidenceClass` 주석, §3 환경 문단(doc-confirmed·fallback 삭제), §7 재사용 표, §8 "runs as real contracts" |
| `docs/specs/USC_ADAPTER.md` | 헤더에 "Legacy v1 reference — superseded by R2, ABI 미확인" 주석 |
| `docs/hackathon/HACKATHON_MVP_SCOPE.md` | 헤더에 R2 note (C07/C08/C09/C10/C13 데모 구성물은 production 불충족, testnet broadcast는 GPU-080 승인 범위) |
| `CLAUDE.md` | 결정 원장/gap 파일 포인터, 다음 진입을 GPU-075/001/002로 갱신 |
| `TICKET.md` | GPU-074 `상태: TODO → DONE` (한 줄) |

편집하지 않은 문서: `PIVOT.md`(제품 근거 기록; §10.2/10.3 충돌은 C02/C03으로 등록만), `docs/hackathon/WHY_WE_PIVOTED.md`(면책 문장 존재, C07 등록), `docs/process/TICKET.md`(레거시 기록 보존), gitignore된 로컬 문서(`DORAHACKS.md`, `DECK.md`, `SCRIPT.md` 등 — 사용자 소유, 미접근).

## 적용한 결정 ID·버전 / 가정 / OPEN 항목

- 적용: 사용자 2026-09-14 R2 요구 + TICKET.md §0.9 → 원장 **R2-v1**로 기록. R2-D01~D14 FIXED.
- 가정: 문서의 환경값(chainKey, precompile 주소, endpoint)은 2026-09-14 문서 확인값이며 런타임 검증값이 아니다(C17; GPU-075).
- OPEN(사용자 결정 필요, 원장 §3): 첫 파트너(R2-O01), 실제 source 지원/증명 가능 사건(R2-O02), past-payout-only 시 admission 보류 vs 상품 변경(R2-O03), 최신성 정책(R2-O04), 정산 레일/대체 체인 구성(R2-O05), 대출 stablecoin(R2-O06), relayer 처분(R2-O07), 해커톤 문서 처리(R2-O08), GPU-080 testnet 예산(R2-O09).

## 완료 기준별 확인 결과

| 완료 기준 | 결과 |
| --- | --- |
| 확정 R2와 사용자 추가 결정이 필요한 상품 변경이 분리된다 | [통과] 원장 §1 FIXED(14) vs §3 OPEN(9) 분리; NFT/미래현금흐름/대체 체인/CTC-ATC는 OPEN 또는 DEFERRED |
| 기존 금융/권리/레거시 요구를 삭제하지 않음 | [통과] PIVOT·TICKET 미삭제; R2-D13 명시; v1 주소/레거시 문구는 planned/unverified로만 표기 |
| 기능 존재를 문서만으로 DONE 처리하지 않음 | [통과] GAP §1: 모든 Attestcoin/GPU 구성요소 PLANNED, 0 LOCAL/NATIVE/LIVE |
| 사용자/동시 실행자의 변경을 덮어쓰지 않음 | [통과] `git status`로 미추적 3개 문서 확인·미변경; 편집은 정확 일치 문자열 치환(각 1회 검증) |
| 공식 검증 필수·Creditcoin execution·자체 서명 fallback 금지·보조 승인 존속을 확정 결정으로 기록 | [통과] R2-D01/D02/D03/D04 |
| 구현/예정/LOCAL/native-testnet/partner LIVE를 파일·artifact·tx 증거로 분류 | [통과] GAP §1 분류 열 + 근거 열 |
| 미구현인데 배포/실행 중이라고 쓴 문장을 planned/unverified로 정정, 외부 배포 부재는 단정 않음 | [통과] C04/C05/C06 편집; C18 v1 "still live"는 UNVERIFIED_EXTERNAL로 등록만 |
| 충돌 목록화 + 담당 티켓 연결(signer/source-native, 낮은 advance rate, NFT lock=실물집행, past payout=미지급채권, notify(balance), tx 단위 replay, 오래된 ABI/endpoint) | [통과] C01~C20; 담당 GPU ID 36개 존재 확인(grep `^### GPU-`) |
| demo NFT를 production 필수 담보로 승격하지 않음 | [통과] R2-D10/D14, C07 |
| 짧은 CLAUDE.md의 R2 진입 안내 | [통과] 포인터·다음 티켓 갱신 |

## 실행 명령·cwd·exit code·test 개수·중요 결과

| # | cwd | 명령 | exit | 결과 |
| --- | --- | --- | --- | --- |
| 1 | root | `git status --short` | 0 | 기존 미추적 3개 문서만; 보존 |
| 2 | root | `cat docs/specs/USC_ADAPTER.md TECH.md README.md docs/hackathon/HACKATHON_MVP_SCOPE.md`; `sed -n` PIVOT §1/§2/§6.2/§10/§12 | 0 | 충돌 원천 확인 |
| 3 | root | `grep -rniE "attestcoin\|usc[-_ ]?sdk\|blockprover\|…0fd2\|gluwa"` (코드 확장자, deps 제외) | — | 코드 0 파일 |
| 4 | root | `grep -rn "grantTestnetCredit\|register-and-grant" contracts/ offchain/api/hashcredit_api/ script/ apps/web/src` | 0 | v1 코드에 존재 (C16 → GPU-001) |
| 5 | root | `grep -rn "0x593e14…2055"` | 0 | README/TECH/.env.example/apps/web env 기본값에 존재; 체인 미조회 |
| 6 | root | 담당 GPU ID 36개 `grep -q "^### GPU-NNN "` | 0 | 전부 존재 |
| 7 | root | `.venv-py313/bin/python scratchpad/edit074.py` (정확 일치 치환, 개수 1 검증) | 0 | README 13, TECH 6, USC_ADAPTER 1, MVP_SCOPE 1 |
| 8 | root | `.venv-py313/bin/python scratchpad/edit074b.py` | 0 | CLAUDE.md 2, TICKET.md 1 |
| 9 | root | `grep -rnE "lower advance rate\|attested adapter\|attested \(not proven\)" README.md TECH.md docs/` (docs/gpu 제외) | — | 잔존 1건 = TECH §2.1 R2 정정 문장 내 부정형(허용) |
| 10 | root | `grep -rnE "runs? as real contracts" README.md TECH.md` | 0 | 2건 모두 "designed to run … not implemented"로 정정됨 |
| 11 | root | `git diff --check` | 0 | clean |

테스트 개수: 해당 없음(SPEC 티켓; 코드 변경 없음). SOL/PY/WEB 재실행하지 않음 — 코드 미변경, GPU-000 기준선(aea2405) 유효.

## 실패/미실행과 이유

- 공식 SDK/ABI/endpoint 명칭 검증 미수행 — GPU-075 범위(외부 자료·artifact 필요). C11은 UNCONFIRMED로만 표기.
- v1 배포 주소·live URL 검증 미수행 — 범위 밖(C18, GPU-060/063). 부재도 단정하지 않음.
- 전 범위 문서 마감 검토 미수행 — GPU-065가 재수행.

## 외부 검증

없음. 네트워크 broadcast·RPC·파트너 연락 없음.

## 남은 작업·차단 입력·다음 READY 티켓

- 차단 입력: 없음(이 티켓). OPEN R2-O01~O09는 사용자/사업 결정 대기이며 코드 티켓 시작을 막지 않는다.
- 다음 READY: **GPU-075**(선행 GPU-000 충족; 공식 문서/패키지 네트워크 접근 필요 — 없으면 LOCAL 산출물 + BLOCKED_EXTERNAL), GPU-001, GPU-002, GPU-003(SPEC, 선행 확인 필요).

## 재개 시 필요한 파일·실행 중 프로세스/포트·변경 소유권

- 파일: `docs/gpu/decisions/attestcoin-first.md`, `docs/gpu/execution/ATTESTCOIN_GAP.md`, `CLAUDE.md`.
- 실행 중 프로세스/포트: 없음.
- 변경 소유권: 결정 원장 버전 변경은 사용자 승인 후 통합 담당자만; GAP §1 inventory는 GPU-075+ 산출 시 갱신.

## Review

(독립 리뷰 세션이 작성)
