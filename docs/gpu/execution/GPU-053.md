# GPU-053 — runtime / deployment checkpoint

Status: IN_PROGRESS · 2026-09-14 · Codex · LOCAL + observed hosted failure

User authorized commit, push and deployment on 2026-09-14, and explicitly granted
deployment authority without further permission questions. This does not establish
third-party rights, native verification, production credit approval or partner E2.

## GPU-053.a: observed Railway startup failure

The existing `rackline-api` deployment of `4ed5362` was CRASHED before this change.
The public health request timed out. Sanitized deployment logs identified:

```text
/app/hashcredit_api/gpu/runtime.py
REPO_MANIFEST_DIR = Path(__file__).resolve().parents[4] / "config" / "attestcoin"
IndexError: 4
```

`runtime.py` now discovers the manifest directory by existing ancestor paths,
supporting the monorepo and `/app` layouts. Root Dockerfile explicitly copies the
versioned Attestcoin configuration. Missing artifacts produce an empty allowlist;
there is no mock/native downgrade. No signing key, secret value, or contract state
was changed. Existing tests plus four packaging regressions cover the change.

Evidence: LOCAL until the replacement hosted deployment and smoke check complete.
Validation: `.venv-py313/bin/python -m pytest offchain/api/tests/test_gpu_runtime_packaging.py offchain/api/tests/test_gpu_auth.py -q`
from repository root: exit 0, 45 passed / 0 skipped (4 packaging + 41 existing auth/security).
Remaining parent scope: standalone BTC-free GPU API image, DB/migrations, role-separated
workers and signers, official TS worker build, local Compose, profile-readiness checks,
and deployment validation. This checkpoint is not GPU-053 DONE or a native proof PASS.
